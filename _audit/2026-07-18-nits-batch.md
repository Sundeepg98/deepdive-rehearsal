# 2026-07-18 — Nits batch (test-infra / overlay-css / overlay-logic): 3/3 CLEAN, merge-ready

Three deferred-nit lanes built in isolated worktree branches off base `54596e7` (the same base as the QW1–3 wave), each cold-verified by an independent verifier with its own instruments and negative controls on the base build. Coverage complete: 3 expected / 3 delivered, no dropped agents. One bounce total (overlay-css, closed in round 2).

## BOUNCED / UNRESOLVED

**Nothing blocks merge.** Three records to carry:

1. **⚠ nit-overlay's archived gate capture is RED for environment reasons, not code.** The fixer's own `_verify/gate-full.txt` ends `GATE: FAIL` — every Playwright check died with exit `3221225794` (`0xC0000142`, Windows process-spawn fault) in the fixer's environment, so the fixer never evidenced full-gate green. The **verifier ran the full gate clean on the same commit `e3096d1`: GATE: PASS, all 40 checks** incl. visual_regression 16/16 against committed pixels, cta_contrast 36/36, build_determinism, room_contrast. Code is CLEAN; supersede the archived red capture with a green run at merge (the serial merge gate provides exactly that).
2. **nit-test verifier's belt-and-braces full-gate rerun was still executing at report deadline** (task `bfa9tvysr`, output `scratchpad/gate_verifier.txt`). Explicitly non-load-bearing: no lane claim rests on it — the fixer's gate run 2 is **40/40** (`_verify/gate_full_2.txt`) and every protocol-mandated spot (negative control, teeth, cta_contrast ×3, both themes) passed under the verifier's own instruments. Fixer's gate run 1 was 39/40 with the red being click_drift's planted-defect arm under sibling contention — the documented gate-reliability class, attributed with evidence (3× standalone PASS on the same commit, in-gate PASS run 2, and AST+grep proof that the diff has no execution path into click_drift).
3. **Informational scope boundary (not a bounce), for the record:** the new shadow-crossing `waitPainted` walk covers a shadow child under fading light-DOM ancestors (the observed failure class) but NOT the dual case — slotted light-DOM content under a fading shadow-INTERNAL wrapper. Outside the lane's claim; no current consumer has slotted targets.

## Verdict table

| Lane | Branch | Commit | Verdict | Rounds | Key numbers |
|------|--------|--------|---------|--------|-------------|
| test-infra | `qw3/nit-test` | `52507c2` (= `e63abc9` + `52507c2` on `54596e7`) | **CLEAN** | 1 | Old walk **false-greened a held shadow child in 21–88ms (computed 1.0, truth 0.5/0.56)**; on the real page at #mockopen-ready, `.push` true effective opacity **0.89–0.985 while old walk said 1.0000 — below the 0.95 CORE_ALPHA gate in 4/12 contexts** (verifier, stronger than fixer's sample); new walk refuses then converges 602–720ms after release; light-DOM chains identical + `===` product; cta_contrast **14/14** (fixer) + 3/3 vs 2/2 old interleaved (verifier), inside-waitPainted time **1498/1899ms new vs 3554/2075ms old**; comment edit **provably comment-only (Python AST bit-identical)**; gate **40/40** run 2 |
| overlay-css | `qw3/nit-css` | `85a50ef` (r1 `d575808` amended; parent `54596e7`) | **CLEAN** | **2** | Verifier **117 PASS / 0 FAIL across 14 arms** (3 builds × 2 vp × 2 themes + 2 reduced-motion); **28/28 opens topGap=18**, one width 560/324 ×7, bottom-flush **14/28 → 0**, bottomGap≥18, radius 18 ×7, shadow+backdrop computed-identical ×7; pills **21→24px desktop / 44px @360** (r1 regression 24/24 reproduced as negative control, r2 fixes); 2 re-captured baselines pixel-confined — **all 1238/1244 changed px inside the claimed 931×21 box at (170,779), zero elsewhere**, 13 others byte-identical; gate **40/40 ×2**, deadzone 35, keyboard 47; 3 prose figures corrected to committed JSONs |
| overlay-logic | `qw3/nit-overlay` | `e3096d1` (series `2c75394` → `6a6f36b` → `e3096d1`) | **CLEAN** | 1 | Base negative control: #keyopen `display:none` + real tap TIMES OUT, grid **322 vs 339 (17px clip)**, raw UA focus ring, sheet overflows 415px with no cue. Fixed: touch-only tap path opens keyov at **360×740 AND 390×844**, 322=322 / 352=352, verifier 420px boundary probe 3 tracks 382=382, desktop 558=558 unchanged; ED3 ring **6/6 room×theme == resolved `var(--acc)`**, mouse control fv=false; cue 24px/36px → 0/0 at end, returns on scroll-up, reduced-motion identical, gradient seamless both themes; lane proof **41/41 ×2**; keyboard **47/47** + deadzone **35/35** standalone; **verifier full gate 40/40**; stylelint 54=54 identical set; post-gate rebuild **byte-reproduced the committed deliverable** |

All three: not merged, not pushed, main repo tree untouched. Verified against base `54596e7`.

## Per-lane: what actually changed

### test-infra (`qw3/nit-test`) — two hardenings, both with proven teeth

1. **`e63abc9` — check_all.py stops citing a flaky total as the pre-fix truth.** The click_drift comment read "FAILED 14-15 of 128 (timing-dependent)"; it now cites the DETERMINISTIC BACKBONE per the ndrift audit (`2026-07-13-ndrift-code-lane-verdict.md` Item 3): **9 tab-drift + 1 behavioural assertions fail on EVERY pre-fix run** — 55.6px is a static layout measurement with no timing in it, so it can never false-green — with the totals demoted to observations (11 under load, 14–15 idle) and an explicit "no pre-fix total is an invariant; the 10 are." Verifier proved it comment-only: Python AST of base vs committed file bit-identical (18273=18273), ASCII-clean, zero execution-path change.
2. **`52507c2` — `waitPainted`'s effective-opacity walk crosses shadow boundaries.** Sole code change: the walk step `parentElement` → `parentElement || getRootNode().host || null`. **Why it's not cosmetic:** on the real deliverable, at the instant `#mockopen` read ready, a light-DOM `DIV#drill` fade **above the deep-drill shadow host** meant `.push`'s true effective opacity was 0.8937–0.9850 while the old walk (blind past the shadow root) reported 1.0000 — in 4/12 theme×room contexts that truth is **below the 0.95 CORE_ALPHA pixel gate**, i.e. the exact "no core glyph pixels" flake class `9c12244` fixed for `#mockopen` was one load-shift from a red on `.push`. Teeth proven on synthetic fixtures (fixer's and verifier's independently): a shadow child under a held light-DOM `opacity:.5/.56` ancestor — old walk false-greens in 21–88ms computing 1.0; new walk computes the true product bit-exactly, **refuses while held, then converges 602–720ms after the hold releases** (waits, not permanently red). Light-DOM callers behaviour-identical **by construction** (the hop clause is reachable only at `<html>`, where `document` has no `.host`) **and by proof** (identical walked chain `SPAN>DIV>DIV>BODY>HTML`, `===` bit-identical product from both impls; `#mockopen` old/new identical in 12/12 contexts). Sole consumer `cta_contrast`: PASS across fixer's 14 runs and verifier's 3 new-walk / 2 old-walk interleaved runs, contrast bands identical — and instrumented time INSIDE waitPainted **dropped** (1498/1899ms new vs 3554/2075ms old over 36 waits/run); wall-clock spread was concurrent-lane box load.

### overlay-css (`qw3/nit-css`) — the overlay-family unification, then a bounced-and-closed round 2

**Round 1 (the unification argued).** Base state: the dialog family was three families — the Cram panel sat at **600px wide / top-18 on a `display:block` container while the six mock dialogs sat at 560px / top-36**; at 360px there were THREE different vertical anchorings (36/gap, 36/flush, 18/gap); **14/28 opens sat bottom-flush** (0px to the viewport edge); short dialogs left a 344–511px dead-backdrop band. Shipped argument: **one family, one frame** — a single width (560 desktop / 324 @360) ×7 dialogs, a single **top-hung anchor topGap=18 on all 28 opens** (7 dialogs × 2 themes × 2 viewports), radius 18 ×7, box-shadow + backdrop-filter computed-identical across the family, both containers flex/flex-start, bottom-flush 0 with bottomGap≥18 (tall dialogs 18/18 symmetric). **Top-hung over centered is load-bearing, not taste:** mockDynamics' measured y stays fixed at 18 while its content height grows +150.2px — growth extends downward from a stable top edge instead of re-centering jitter on every reveal. Pill heads also gained a 24px desktop floor (was 21px).
**Round 2 (`85a50ef`, amended onto r1 per verifier instruction) — both bounce items closed:**
1. **Mobile tap-floor regression fixed.** R1's 24px pill rule had clobbered the 44px mobile floor (verifier's negative control reproduced it: 24/24 @360 on `d575808`). Fix: re-assert `.ix-g-cram{min-height:44px}` **inside the existing ≤919px MOBILE TAP FLOOR block** (styles.css:1524) — the block's own pattern for controls whose (0,1,0) class rule beats the bare-button (0,0,1) floor. **Chosen over `min-width:920` scoping of the base rule to avoid the 919–920px fractional sliver where the pill would carry no floor at all.** Proven with a committed instrument (`_audit/2026-07-18-nit-css/pills-vp.cjs`) across 3 builds × 2 surfaces × 2 themes × 2 viewports, rect and offset boxes agreeing 8/8 arms per build: parent 21/44 → r1 24/24 → fixed **24 desktop / 44 @360**, layout intact.
2. **Prose reconciled to the committed evidence JSONs**, catching two more unsupported figures the same way: bottom-flush 9/28 → **14/28** (membership verified from before.json and identical to the verifier's live re-measure), void range 332–513 → **332–491** (513 never existed in the evidence), mixed-fire span 252.4–371.8 → **278.5–424** (pooled 8 committed opens; verifier's own samples confirm a random-redraw distribution). Corrections disclosed in the amended message.
CSS-only proven end-to-end: deliverable diff lines byte-identical to the src/styles.css diff (36 lines vs parent), no JS/DOM files in the commit, print path guarded by the existing `!important` overrides; desktop dialog frames r1==fixed (the amend is mobile-pill-only); no computed-value churn base→fixed on radius/backdrop/shadow/title per dialog per arm.

### overlay-logic (`qw3/nit-overlay`) — reachability, focus theming, scroll cue

1. **P2 / row 14 — keyov touch reachability (the design chosen).** Rather than inventing a new mobile affordance, the fix makes the existing Tools-menu entry touch-visible: **removed `kbd-only` from `#keyopen`** (src/index.html) and **deleted the then-zero-consumer `@media(max-width:919px){.kbd-only{display:none}}` block** (styles.css v99) — the shortcuts overlay is now reachable by touch alone (proof: hasTouch contexts, `page.tap` Tools → #keyopen → keyov open + `aria-hidden=false`, at 360×740 AND 390×844; base control: computed `display:none` and the tap times out). The 17px grid clip at 360 (322 vs 339) is fixed by an **explicit 2-column media query inside the shadow sheet at max-width:419px — deliberately NOT `auto-fit`, which would refit desktop to 4+ columns**; desktop is a proven control (3 tracks, 558=558, #keyopen `display:flex`, identical numbers both builds), and the verifier added a 420px boundary probe the fixer never ran: 3 tracks, still fits (382=382). `kbd-only` survives only in tombstone comments.
2. **ED3 — themed focus ring on the run surfaces.** `.mock-body:focus-visible{outline:2px solid var(--acc);outline-offset:-2px}` added to MOCK_SHEET — **offset inward because the surface sits flush to the panel's rounded corners**; covers `#mockbody` now and `#mixbody` by class (CSSOM-asserted in `<deep-mixed-fire>`'s adopted sheets). 6/6 room×theme combos: keyboard-open ring == `var(--acc)` resolved in-shadow (light rgb(150,61,134)/rgb(146,78,0)/rgb(49,91,180); dark rgb(218,141,202)/rgb(225,149,86)/rgb(125,166,243) — fixer's cited values matched exactly), `:focus-visible` true; mouse-open control fv=false, outline none; matches the styles.css:53 `button:focus-visible` family. Base control: raw UA ring `auto 1px rgb(16,16,16)`, rule absent from both shadows.
3. **Row 17 — mobile Tools sheet bottom scroll cue.** The shipped v90 seg-scrim recipe rotated vertical: shell.js's v75 block maintains `--fb` (24px/0px, sibling of v80's `--fl`/`--fr`), a mobile-only `.sidebar .mockbar::after` gates its HEIGHT off the flag, the gradient fades to the sheet's OWN `var(--card)` (seamless both themes — dark rgb(30,28,37)==rgb(30,28,37)), `position:fixed` rides the sheet through its transform. At 360×740 with 482px below the fold: ON at start and mid (`--fb`=24px, ::after h=36px), OFF at end (0/0), **returns on scroll-up**, identical under emulated reduced-motion (the cue itself is static; only the height transition exists and collapses to 1e-05s), desktop `::after` `content:none`.
Hygiene: diff = exactly 6 files (index.html, styles.css, keyboard-overlay.js, shared-sheets.js, shell.js, deliverable rebuild); zero non-ASCII bytes; stylelint **54=54 verified as an identical SET** (48 warnings pairing 1:1 by rule+text, only line numbers shift); sibling-lane scope respected (**zero diff hits on `.cram-panel`/`.mock-panel`/`.pill`**); the verifier's post-gate `git status` still showed only untracked `_verify/` — the gate's rebuild **reproduced the committed deliverable byte-for-byte**.

## Baseline changes

| Lane | VR baselines | Justification |
|------|--------------|---------------|
| test-infra | **None** — no `vr:update`; VR 16/16 worst-0px in both gate runs | Test-infra only; deliverable untouched. |
| overlay-css | **2 of 16 re-captured in r1** (home-light, home-dark) + their 2 manifest sha lines; **zero r2 touches** | One cause: the pill row crossing the 800px fold. Fixer: 1218/1220 px (comparator channelTol=2) inside one 931×21 box at (170,779). Verifier's independent PNG decoder at **zero tolerance: 1238/1244 changed px, ALL confined to exactly that box, zero elsewhere in either PNG**. 13 other baselines byte-identical; walk-dark restored to the committed PNG after rejecting sub-tolerance noise. The r2 ≤919px re-assert changes nothing VR captures (the two 390×844 m-walk baselines show the walk pane, which has no pills). |
| overlay-logic | **None** — no baseline touched, no `vr:update` | All three changes invisible at captured rest states: keyov/tools-sheet/focus states aren't in the 16-baseline set, sheet closed at rest on m-walk, ring is `:focus-visible`-only, desktop pixels untouched by scoping (kbd-only rule only ever acted <920px, grid change <420px, scrim pseudo mobile-scoped + `content:none` on desktop) — and the verifier's full gate confirmed VR 16/16 against committed pixels. |

## Pre-merge notes

- **nit-test is disjoint** — tracked changes are exactly `test/check_all.py` + `test/_boot.cjs`, no deliverable rebuild; merges cleanly against anything, any order.
- **nit-css and nit-overlay both touch `src/styles.css` and both rebuilt the deliverable** → pairwise deliverable conflict; resolution is the established recipe (merge sources, regenerate the deliverable — `2026-06-29-batch1-verification.md`). Source overlap is different regions (overlay-family frame + ≤919px tap-floor block vs the kbd-only block deletion + scrim recipe); nit-overlay's verifier proved zero diff hits on nit-css's selectors.
- **VR ordering is flexible:** only nit-css changes baselines (the 2 home PNGs); nit-overlay proved 16/16 against committed pixels independently and nit-test is pixel-neutral. Each serial merge gate run confirms.
- **Run-of-record:** take the serial merge gate's quiet-box 40/40 per merge — that simultaneously supersedes nit-overlay's environment-red archived gate capture (item 1 above) and moots nit-test's still-running verifier belt-and-braces rerun (item 2 above).

## Evidence

- **Worktrees:** `D:/claude-workspace/_worktrees/deepdive-rehearsal/{nit-test,nit-css,nit-overlay}` — tracked files clean at HEAD on all three; `_verify/` evidence untracked by lane convention.
- **Committed instruments:** nit-css `_audit/2026-07-18-nit-css/pills-vp.cjs` + evidence JSONs (`pills-parent/r1/after.json`, before/after.json) with honest build provenance (`parent-54596e7.html` / `r1-d575808.html`).
- **Fixer evidence (untracked, in-worktree):** nit-test `_verify/waitpainted_shadow_proof.{cjs,txt}`, `cta_timing_ab.txt`, `clickdrift_standalone_{1,2,3}.txt`, `gate_full.txt`, `gate_full_2.txt`, `ab/`; nit-css `shots-r2/`; nit-overlay `_verify/nit_overlay_prove.cjs` (41 assertions ×2), `cue_dark.cjs`, 14 shots, `gate-full.txt` (the environment-red capture, superseded).
- **Verifier instruments (session scratchpad, ephemeral):** nit-test `fixture.html`, `verify_shadow_walk.cjs`, `real_instant_probe.cjs`, `cta_{n1,o1,n2,o2,n3}.txt`, `gate_verifier.txt`; nit-css `verify-ov.cjs`, `assert-ov.cjs` (117/0), `pngdiff.cjs` (own PNG decoder), `verify-ov-results.json` + `shots/`; nit-overlay `nit_overlay_verify.cjs`, `results-{base,fixed}.json`, 14 shots in `shots-{base,fixed}/`.

*Batch synthesized 2026-07-19 from per-lane fixer reports + independent cold verdicts (test-infra 1 round, overlay-css 2 rounds, overlay-logic 1 round).*
