# D3-perf wave — first-visit costs (attribution-first) — freeze report (2026-07-20)

**Branch:** `perf/d3-first-visit` off `master` @ `dfbfe9d` (the correctness-wave merge).
**Author:** d3-fixer. **Specs:** `_audit/2026-07-19-postship-cold-audit.md` (items #2/#8/#9),
`_audit/2026-07-18-midrange-perf.md` (the perf track's history + its three deflation errata).
**Status:** DRAFT — attribution complete; mechanism decision pending the ceiling/A-B measurement.

The brief's law for this wave was **ATTRIBUTION BEFORE MECHANISM** on all three items, with an
explicit warning that this track "once inverted its blame entirely," and that "an evidenced no-op
is a VALID outcome." Both proved load-bearing: the attribution **inverts the audit's proposed
mechanism on all three items**.

---

## 1. Instrument (how the numbers were taken)

- Repo Chromium (playwright 1.61), `file://` the built 11.7MB deliverable, launched with the gate's
  own `_boot.cjs` (`launchOpts`/`gotoApp`/`enterApp`/`settle`) so boot/readiness match the house.
- CPU throttle via CDP `Emulation.setCPUThrottlingRate` (4x), **verified each cycle by a calibration
  spin** (a fixed busy loop, JIT-warmed): its 4x/1x ratio must be ~4, and its absolute 4x time is the
  per-cycle **LOAD covariate** — an inflated spin means a loaded box.
- **First-visit semantics:** each pane's first activation is measured in its OWN fresh reload (the
  target is the 2nd pane of the session, after walk), so no warm layout leaks between samples.
- **Metric:** in-page wall-clock. `t0` just before a synthetic click through the real
  `onclick→goView→Router.navigate→emit→switchTab` path (all synchronous — verified in `router.js`
  `emit()` and `view-transitions.js` `run()` which no longer defers), then rAF to first painted
  frame. `sync` = the click task (JS + any forced layout); `toPaint` = input→first paint (what the
  user feels).
- **Attribution:** the Chromium timeline trace (`browser.startTracing`) decomposed by self-time into
  script / style-recalc / layout / paint. **Never the longtask API** (it drops big throttled tasks at
  4x — this track's documented measurement trap).
- Scripts live untracked in `_perf/` (`attrib.cjs`, `trace.cjs`, `ab.cjs`, `ceiling.cjs`); raw JSON in
  `_perf/results/`. The base build is snapshotted at `_perf/builds/base.html` (sha256 verified ==
  HEAD's committed deliverable).

**Load honesty (up front):** the box is shared by four builders and was **loaded for these runs**
(calibration ratio ~5.8 vs the ~4.0 of a calm box; 4x spin band 95–369ms vs the ~158ms calm point).
Per this track's law and its three errata, the **absolute milliseconds below are load-inflated**; the
**decomposition and rankings are load-invariant** and are the durable content. Deltas are reported as
matched-load paired A/B (the track's own resolution: "matched-load ratios govern").

---

## 2. ATTRIBUTION — the audit measured the symptom, mis-attributed the mechanism

### Item #2 — drill first-activation (the P1). Audit hypothesis: *"prewarm probe 1 so activation is a display toggle"* (i.e. `drawCard()`/`_mustHit()` script is the cost).

**REFUTED. The cost is reveal LAYOUT, not script.** Trace self-time of a single drill first-activation
(@4x, 3 reps, under load):

| term | ms (self, p50) | what it is |
|---|---|---|
| **Layout** | **~217** | laying out the drill shadow subtree the instant `.pane.on` toggles `display:none→block` |
| UpdateLayoutTree (style recalc) | ~57 | style resolution over the revealed subtree |
| EventDispatch (self) | ~38 | the click dispatch machinery |
| Paint / PrePaint | ~26 | |
| **FunctionCall (all JS, incl. `drawCard`+`_mustHit`)** | **~3.8** | the script the audit blamed |

Mechanically why: `drawCard(0)` already runs **eagerly at topic-load** (`DeepDrill` is `eagerTopic`;
`renderTopic → setMode('study') → renderD → drawCard(0)` builds the first card into the still-hidden
shadow root), and `_mustHit` only runs at the final reveal stage (not stage 0). So the card DOM
already exists before first activation; the activation pays the **reveal layout** of a previously
`display:none` subtree. `sync ≈ toPaint` (99%) confirms it's forced synchronously inside the click
task, not deferred to the render phase. **Prewarming `drawCard` would save ~4ms of a ~275ms cost** —
and would needlessly reach into `drawCard`'s region (owned by the product wave) for no gain.

### Item #8 — five panes first-visit (trade/model/wb/num/sys). Audit hypothesis: *"idle-prebuild the next-likely pane."*

**INFEASIBLE as stated. Same cost (reveal layout), and you cannot prebuild it.** Every pane's
first-activation `sync ≈ toPaint`, i.e. the cost is the same forced reveal layout of its shadow
subtree. The RENDER is already deferred-then-done by `TopicPaneQueue` (the panes' innerHTML exists in
their shadow roots by the time they're first shown); what's expensive is the **LAYOUT**, and a
`display:none` subtree **cannot be laid out ahead of time** — layout is skipped precisely because it's
`display:none`. "Idle prebuild" addresses a render cost that is already paid; it cannot touch the
layout cost that remains.

### Item #9 — topic-open variance (180–332ms). Audit hypothesis: *"defer/chunk the bank reseed off the critical path."*

**REFUTED. The cost is the room-retint style recalc + walk relayout, not the reseed.** Trace
self-time of a topic switch (@4x, 3 reps, under load):

| term | ms (self, p50) | what it is |
|---|---|---|
| **UpdateLayoutTree (style recalc)** | **~146** | `applyIdentity` sets `data-group` on `<html>` → the six-rooms accent re-tint → full-document style recalc (every `var(--acc)` consumer) |
| **Layout** | **~110** | the visible walk pane + identity header relayout |
| Paint / ParseHTML | ~42 | |
| **FunctionCall (incl. `publishBanks` — the "reseed")** | **~5.7** | the script the audit blamed |

`publishBanks` (the bank reseed) is **5.7ms of array `.slice()`/`.map()`**. The real cost is the
**room re-tint style recalc**, which is inherent to the *shipped, test-guarded* six-rooms accent
system (`data-group` → every accent token retints; do NOT rebuild — see the project's accent-rooms
note). The **variance** the audit flagged is substantially **box-load contention**, not a fixed
deferrable cost: on a calm box the topic-open tightens.

### One-line summary
**All three audit perf mechanisms are inverted or infeasible.** The costs are `display:none`→visible
**reveal layout** (panes) and the six-rooms **retint style recalc** (topic-open) of a very large
single-file document — not the script-level operations (`drawCard`, prebuild, reseed) the audit named.
This is the "CONFIRMED mechanically, INVERTED financially" pattern the perf track already has on
record, one level deeper.

---

## 3. Mechanism — `content-visibility:auto` on the two flagged card-list breaches; evidenced no-op on the rest

The only attribution-supported lever for the reveal-layout term is `content-visibility:auto` on
below-fold pane cards (candidate #3 from the 2026-07-18 attribution): it lets the browser skip the
layout of cards below the fold on the first reveal, laying out only what's on screen. VR is captured
at the **viewport** (1280×800), so below-fold cards it skips are already outside the capture — the
at-rest pixels are unchanged.

### The CEILING test (runtime-injected, zero build) — which panes the lever actually reaches

Injecting `content-visibility:auto` into a pane's shadow root right before its first activation, paired
against the un-injected reveal on the **same** build under the **same** load (matched-load paired A/B,
the delta cancels box load). `d` = injected − baseline (negative = faster); MATCHED = cycles whose
base/branch load spins agree within 15%:

| pane (selector) | matched d (ms) | n | verdict |
|---|---|---|---|
| **trade** (`#tdecs > .dec`) | **−250 to −325** | 3 | huge win — a clean vertical card stack |
| **rf** (`#rflist > *`) | **−137** | 3 | big win — but rf is NOT a breach (~80ms calm, the audit's fast half) |
| **num** (`#out > *`) | **−28** | 8 | modest win — a flagged breach (150ms) |
| **sys** (`#chain > *`) | +8 | 6 | NO win — content fits within ~1.5 viewports |
| **wb** (`#list > *`) | +168 (n=1, noisy) | 1 | NO win |
| **drill** (`.dnav-wrap` block) | **−12** | 6 | NO reach — the drill P1's cost is ABOVE-fold |
| **drill** (`#dnav .dn-step` grid items) | −11 | 6 | NO reach (grid items don't skip) |

**The drill P1 is not addressable by this lever.** Its ~250ms reveal is above-the-fold shadow-subtree
layout+recalc (scoreboard, mode/tier toggles, first probe card) — content-visibility can only skip the
`.dnav` below the fold, which is ~11–12ms of the cost. Fixing the P1 would mean restructuring the
drill's visible content (out of scope — the product wave owns drawCard's region) or a broader shadow
change; neither is justified for a first-visit-only, load-inflated, one-time-per-topic cost.

### What shipped (numbers-justified, disciplined)

`content-visibility:auto` + `contain-intrinsic-size:auto <h>` on the below-fold card lists of the
**two flagged item-#8 breaches where the lever both works and clears/moves the bar**:

- **`deep-trade-offs` `.dec`** (`trade-offs.js`) — the heaviest first-visit pane (217ms); the lever
  takes it under the comfort bar. Trade is NOT in the VR baseline set, so its zero-VR is proven
  **directly** (`_perf/vrcheck.cjs`: capture trade at rest, base vs branch, 1280×800 light+dark, diff
  with the gate's own CHANNEL_TOL=2 — [result below]).
- **`deep-numbers` `.nrow`** (`num/logic.js`) — a flagged breach (150ms), modest win, and **VR-guarded**
  (`num-light` baseline) so the gate itself proves zero pixels move. [Kept iff the real-build A/B holds;
  see §3 A/B table.]

Both carry a `@media print{… content-visibility:visible}` guard so a printout is never missing a
below-fold card. Neither touches drawCard's reveal region, home, search (product wave's), or the
six-rooms retint (the topic-open cost).

**NOT shipped (evidenced):** rf (a real win but not a breach — gilding a non-problem), sys/wb/model
(no lever), the **drill P1** (no reach), and **topic-open** (the cost is the inherent, shipped
six-rooms retint recalc — item #9 is an **evidenced no-op**, which the brief explicitly blesses).

### Real-build matched-load A/B (base build vs branch build) — [PENDING CALM BOX]

The ceiling proves the lever's potential; the ship proof is base-build vs branch-build. The box was
saturated (four parallel builders; calib ratio ~5.8, ~80 node+chrome procs) — the VR gate and full
gate capture-timed-out on readiness, a pure load artifact. Per the track's law these run on a **calm
box** (naturally available after the waves ahead merge). The matched-load paired A/B (load-robust)
and the VR/gate proofs land here on the calm re-run.

*(A/B table + VR-delta result + final num keep/drop + gate verdict land here.)*

---

## 4. Invariants
- judge→advance hot path, `pos.<id>` cursor-restore, flow/nextUp semantics: UNTOUCHED (no change to
  those paths — the diff is two shadow-sheet CSS additions + the rebuilt deliverable).
- **content-visibility jump-path regression class — CLEARED.** `content-visibility:auto` is a known
  regression risk for programmatic scroll-to-below-fold-target, and this app has three such paths
  (team-lead caution): the drill `dn-step` nav, the whiteboard grade→next-cue auto-scroll, and
  walkthrough step anchors. **None intersect this ship:** those live in drill/wb/walk, and the diff
  touches only trade + num (`git diff --name-only` = trade-offs.js, num/logic.js, deliverable).
  trade and num have **no** `scrollIntoView`/`scrollTo`/`focus`/anchor code of their own (grep-verified
  — the only "scroll" tokens in either file are these comments). So no jump path runs under
  content-visibility. (This is also why the drill P1 stayed a no-op: not touching drill avoids both the
  above-fold-cost dead-end AND its dn-step jump path.)
- Zero-VR-delta: num is VR-guarded (`num-light`) → the gate proves it; trade is unguarded → proven by
  `_perf/vrcheck.cjs`. [asserted with the calm-box gate result at freeze].
- All 49 gate checks: [to be asserted at freeze on the merged tree].

## 5. How to reproduce (the cold verifier re-runs these)

The instrument is committed under `_perf/` (`builds/`+`results/` gitignored). Node = the repo's
Playwright 1.61 Chromium; run from the worktree root. Snapshot the two builds first:
`cp <master deliverable> _perf/builds/base.html` and `cp <this branch's rebuilt deliverable>
_perf/builds/branch.html`.

- **Attribution band** (per-pane first-activation, 4x, calm-cutoff filtered):
  `node _perf/attrib.cjs --samples 10 --panes drill,trade,model,wb,num,sys,walk`
- **Trace decomposition** (script vs style vs layout vs paint self-time):
  `node _perf/trace.cjs --pane drill --reps 3` (or `--pane topic`)
- **Content-visibility ceiling** (runtime-injected, zero-build, matched-load — the lever's ceiling):
  `node _perf/ceiling.cjs --cycles 12 --panes trade,rf,sys,wb,num,drill`
- **Ship A/B** (base build vs branch build, matched-load paired, load-robust):
  `node _perf/ab.cjs --base _perf/builds/base.html --branch _perf/builds/branch.html --panes trade,num,drill,sys --cycles 10`
- **Trade zero-VR** (the one unguarded pane I touched):
  `node _perf/vrcheck.cjs --pane trade --base _perf/builds/base.html --branch _perf/builds/branch.html`

Every run prints a per-cycle **load covariate** (the 4x calibration spin) and a MATCHED subset
(cycles whose base/branch load agree within 15%); trust the MATCHED column on a loaded box, the raw
band on a calm one. The longtask API is deliberately unused (it drops big throttled tasks at 4x).

## 6. Freeze battery checklist (execute at the merge window, on a CALM box)

The box was saturated during the build phase (220s boot-timeouts on VR/A/B — proven unusable), so the
proofs run once, at the serial-merge window, on the merged tree.

**TAIL RESEQUENCED (team-lead, after adopting this wave's parallelism retro): D3 merges LAST, alone.**
Order is **D4 → D5 → D2 → D3 (me)**. Two consequences: (a) the box is *guaranteed* quiet — the
team-lead fires `_TEAM_LEAD_MERGE_DOWN.md` only on verified quiescence (no gate/VR/cold-verify/build
running anywhere, all other agents parked), not merely on "D2 merged"; and (b) my battery is the
**final integration re-check across all three merged waves**, so the brief's integration obligation and
the calm-box requirement collapse into this one pass.

1. **Trust but sanity-check the signal.** The signal already means quiescence; still confirm with a
   quick `attrib.cjs --samples 1` — the 4x calibration spin must land near 4x (ratio ~4, not ~5.8).
   Clean any orphaned chromium from prior runs first.
2. **Merge master FIRST, then run everything** (team-lead note): `git merge master` — master now
   carries d4+d5+d2 **and** a gitignore for `_TEAM_LEAD_*.md`. ⚠ **An untracked root file DEFERS
   `build_integrity`'s HEAD-match** (this bit the D4 wave), so if any gate runs BEFORE the merge,
   delete my team-lead files first; after merging master they're ignored and harmless. Expect a
   conflict ONLY in the built deliverable (every wave regenerates it) — resolve by taking neither and
   REBUILDING; my src files (trade-offs.js, num/logic.js) are unique to me and should merge clean. If a
   real src conflict appears in trade/num, STOP and report (ownership breach).
3. **Rebuild + snapshot the A/B pair:** `npm run build`; then
   `git show master:deepdive_content_pipeline_rehearsal.html > _perf/builds/base.html` (merged tree
   WITHOUT my change) and `cp deepdive_content_pipeline_rehearsal.html _perf/builds/branch.html` (WITH).
4. **VR zero-delta:** `node test/visual_regression.cjs` — MUST be green (num is guarded → proves it).
   Then `node _perf/vrcheck.cjs --pane trade` for the one unguarded pane I touched → 0 pixels.
5. **Full 49-gate:** `npm run build && python3 test/check_all.py` FOREGROUND (~20min), verdict from the
   **capture file**, never a piped exit code. One quiet re-run before believing any red.
6. **Calm A/B headline:** `node _perf/ab.cjs --base _perf/builds/base.html --branch _perf/builds/branch.html --panes trade,num,drill,sys --cycles 8`
   (drill+sys = no-regression controls). Plus `attrib.cjs` for the calm absolute band.
7. **num keep/drop — MECHANICAL RULE (pre-committed):** KEEP num iff its matched dPaint ≤ **−15ms**
   AND the VR gate is green AND the full gate is green. Otherwise DROP num (revert the `.nrow` edit,
   rebuild) and ship trade-only. Trade ships regardless (its win is an order of magnitude larger and
   VR-clean). Ceiling said num −28ms, so the default expectation is KEEP — but the real-build A/B, not
   the ceiling, decides. Team-lead ratified that a **DROP is fully legitimate** for a modest win that
   the calm numbers don't hold up; **report the measured band either way** (never just the verdict) —
   this track's deflation law applies to my own result too.
8. **Finalize** §3 (A/B table) + §4 (VR/gate assertions), then **freeze commit** + `SendMessage`
   team-lead. Leave `_perf/*.cjs` committed for the cold verifier (done).
