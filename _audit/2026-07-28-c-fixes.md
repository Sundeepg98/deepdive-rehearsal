# Wave C part 2 — the cram fixes

**Branch** `content/c-fixes` off master `521663d` · **2026-07-28** · content + one composer change
**Gate:** 54/54 PASS, 0 FAIL, 0 SKIP · **VR:** 16/16 baselines matched, zero churn
**Ratchet: 66 -> 0. `test/cram_surface_debt.json` is `{}`.**

Commits: `3f10514` composer · `dd3c42f` when-conj · `1fba187` dup + dangling · `682d0fc` Class E tells · this record.

---

## 0. The headline

The baseline said 66 defects across 35 of 46 topics. It is now zero, and **43 of them were
retired by a single change to one line of the composer** — not by 43 content rewrites. The other
23 were genuine per-item work.

The number that matters more than 66 -> 0 is this one: **`feature-flags` was listed CLEAN in the
baseline, and its sheet was carrying at least five detached answers.** The cold-verify found them
(F3) and the checker structurally cannot. A per-item fixing pass driven by the 66-entry work-list
would have left every one of them in the shipped artifact. The composer route fixed them without
anyone naming them. That is the whole argument for taking the design call, and §2 is its receipt.

---

## 1. PART 1 — the composer fix

### The change
`cram-derive.js` lifted `wb.steps[].a` and dropped `wb.steps[].c`. `wb.steps` is `{c, a}` — a cue
and an answer — so the sheet printed answers with their questions removed. It now lifts both:

```js
function _csCueLine(step) {
  var a = (step && step.a) || '', c = (step && step.c) || '';
  if (!c) return a;
  return '<span class="cs-cue">' + c + '</span><span class="cs-arr">&rarr;</span>' + a;
}
```

Rendered: *"Step order — which steps even need a compensation? **→** Only the ones before the
pivot."*

**Rendering reuses the sheet's own vocabulary.** `.cs-arr` is the identical separator `.cs-dec`
(option → option) and `.cs-trap` (bad → fix) already use, so "X → Y" was already this sheet's core
idiom, twice. One new rule was added, inside `CS_SHEET`'s own scope:

```css
.cs-cue{color:var(--mut2)}
```

`--mut2` is an existing theme token already consumed by `.cs-arr`, `.cs-arr2` and `.cs-dim` on
this same surface, so it introduces no new colour pair and needs no light/dark variant — the
file's stated invariant. Screenshotted in **both** themes: the cue reads quieter, the answer keeps
`--ink`. `.cs-dim` was considered and rejected: it is a block-level micro-note style
(11px vs the line's 15px), and shrinking the prompt inside its own line would read as a rendering
accident rather than a hierarchy.

**Coverage is uniform:** all 415 whiteboard steps across all 46 topics carry a cue (measured, not
assumed), so no topic degrades to the no-cue branch.

**Scope of the ruling.** The brief's ruling also mentioned the `open.cards` items. Those are
**not** included, deliberately: an opener item's keys are `{n, ht, a}` — there is no question
field to lift. Their framing is the section heading ("The one-liner", "the 30 seconds"), which
already supplies it. The two opener `dangling` entries were therefore genuine content defects and
were fixed as such in Part 2.

### Receipt 1 — the ratchet
`dangling` 48 -> 2 in one build. 46 baseline entries went stale simultaneously; **0 new defects,
0 mirror-drift.**

### Receipt 2 — I caught the fix about to launder two real duplicates
The checker mirrors the composer, so the mirror had to move too. The trap was in *what unit* the
mirror feeds each detector. Measured, before changing anything:

| pair | dup on the raw answer | dup on the composed line |
|---|---:|---:|
| `notifications` wb.steps[7].a + opts[1].when | 0.682 **fires** | 0.577 **silenced** |
| `rules-engine` wb.steps[4].a + rf.flags[2].fix | 0.722 **fires** | 0.565 **silenced** |
| `autoscaling` wb.steps[6].a + rf.flags[4].fix | 0.912 fires | 0.775 fires |

Prepending the cue adds tokens to one side of the Jaccard and drags the similarity down. Two
genuine duplicates would have gone quiet **without one word of the duplicated text changing** —
the composer fix silently marking its own homework. So the mirror now measures two units:

- **`dangling` -> the composed LINE.** It asks *"does what the reader meets stand alone?"*, and
  the cue is now part of what they meet.
- **`dup` -> the ANSWER alone.** It asks *"does this sentence reach two sections?"*, which is a
  fact about the sentence, not about its neighbours.

Both duplicates stayed live and were fixed at the content level in Part 2.

### Receipt 3 — a fourth mirror guard, proven able to fail
Reverting the composer to lifting `.a` bare slips guards 1-3 completely: same section set, same
`<li>` count, and the answer text is still present in the sheet. **Worse — because the mirror
composes the line itself, `dangling` would have stayed green at 2 while the sheet showed bare
answers again.** Guard 4 asserts the composed line appears in the rendered sheet.

Watched red: reverting `deriveCram` to `d.wb.steps[s].a` and rebuilding yields **415 mirror-drift
failures** (one per step, all 46 topics); restored -> 0.

A negative-control fixture pins the new shape against the old one, so the detector cannot be
loosened until the composed line passes nor tightened until the bare answer stops failing:

```
dangling+only         "Only the ones before the pivot."                               must FIRE
dangling-NEG-cue-lifted "Step order -- which steps... ? -> Only the ones before..."   must be SILENT
```

### Receipt 4 — the F3 false negatives, retired
The cold-verify's F3 named `feature-flags` as a clean-listed topic carrying >=4 detached answers
the signature detector misses ("So" back-references exactly like "Because" but is not in the
opener set; "Data." is opaque cold). Read off the **rendered** sheet after the fix:

| the sheet now reads | F3 status |
|---|---|
| *"Why salt the bucket with the flag key? → **So** cohorts don't correlate across flags."* | retired |
| *"What can a flag NOT roll back? → **Data.** The flag flips code, not the rows already written."* | retired |
| *"What is the retire order, and why? → **Code first, definition second.**"* | retired |
| *"What are the three fallback layers? → In-memory ruleset → persisted local cache → ..."* | retired |
| *"What is the kill switch's real speed? → **Bounded by** the refresh window..."* | retired |

Bonus: `observability` step 5, the control's one arguable case, now reads *"How does the trace
context cross the wire? → Injected on every hop"*. **No baseline entry moved for any of these,
because none was ever in the baseline.** That is the point.

### Three cues reworded, and why the detector was not touched
Once the cue is the line opener, the detector sees cue text. Three cues tripped it and would have
landed as **new** defects:

| topic | before | after |
|---|---|---|
| `saga` wb[7] | "**No isolation** — what actually breaks..." | "**Without isolation** — what actually breaks..." |
| `backpressure` wb[7] | "**You can** neither block the producer nor drop the work. Now what?" | "**The producer cannot be blocked** and the work cannot be dropped. Now what?" |
| `leader-election` wb[8] | "**It works.** Now the leader is the bottleneck..." | "**The election works.** Now the leader is the bottleneck..." |

The baseline report is explicit that arguable hits must be *"fixed by rewording, not by loosening
the detector, because loosening it re-admits the 46 real ones."* Reworded. Each reads at least as
well in the Whiteboard pane it came from, and `leader-election`'s is a genuine improvement — "It
works" back-references the previous step, which is exactly the defect on a sheet read cold.

---

## 2. PART 2 — the per-item entries

### `when-conj` — 12 -> 0, the class is empty
Twelve option bodies wrote their own conjunction into a field both renderers already prefix.

**This is not a cram-only defect.** `trade-offs.js:30` renders these bodies after a literal
`pick when` pill, so every one of them stutters in the **Trade-offs pane** as well — 36 rendered
stutters across three surfaces, not the 24 the checker can see.

| topic | n | fix |
|---|---:|---|
| `microfrontend` | 5 | strip the conjunction |
| `eav` | 2 | strip |
| `iac` | 2 | strip |
| `replication` | 2 | strip |
| `consistency-models` | 1 | **recast** — "when X, Y" leaves a comma splice when you delete "when" |

In every decision touched, the *sibling* options already opened on a noun phrase ("the
**default**.", "**almost always**.", "**internal, authenticated, app-like** surfaces."), so this
made the odd one out parallel with its own list rather than imposing a new house style.

### `dup` 6 -> 0 and `dangling` 2 -> 0
Differentiated by **role**, not by paraphrase — each side now carries something the other does not.

| topic | pair | what changed |
|---|---|---|
| `autoscaling` | rf.flags[4].fix + wb.steps[6].a | Whiteboard keeps the drain sequence you draw; the Red Flag now owns *why a naive drain still drops traffic* (endpoint removal and SIGTERM race in Kubernetes) |
| `rules-engine` | rf.flags[2].fix + wb.steps[4].a | Whiteboard keeps "two provably distinct approvers"; the Red Flag now names the hole self-approval leaves — approver one casting approval two — and moves enforcement onto the approver **set** |
| `multi-region` | rf.flags[5].fix + decisions[4].tell | the tell keeps the judgement rule; the Red Flag now specifies the confidence bar, the fencing and the reversibility |
| `kafka-internals` | items[0] + items[1] | **the sweep's named case.** "One breath" and "what is the shape?" were one sentence with the payoff list reshuffled, printed in the one-liner *and* the 30-seconds sections. The shape answer now answers shape — leaders, groups, offset-per-group |
| `notifications` | decisions[5].opts[1].when + wb.steps[7].a | the `when` recited the cascade the Whiteboard already draws; it is now the **condition** the field is for, parallel with its sibling |
| `backpressure` | items[1].a + decisions[0].tell | cleared by the Class E rewrite in Part 3 — see below |
| `circuit-breaker` | dangling items[1].a | "Not recovering a single call, but..." -> "**The point is** containing the blast radius, not recovering a single call: ..." |
| `saga` | dangling items[1].a | "It's ACD without isolation..." -> "**A saga is** ACD without isolation..." |

`backpressure`'s dup was deliberately held back one cluster: its other half was on the Class E
work-list, so fixing it in Part 2 would have meant editing the same string twice. Rewriting the
tell dropped the pair below `DUP_MIN` **honestly — by changing the duplicated content**, not by
diluting the measurement.

---

## 3. PART 3 — Class E, the Trade-off tells

`cram-derive.js:130` promotes every tell verbatim into *"Senior tells — say these"*, so the
weakest string in a topic file is mechanically published on the highest-leverage surface.

### Scope, stated honestly
The sweep named ~36 tells across 22 topics. **I read all 155 tells in those 22 topics** against
the sweep's own test — *can anything in this flip the call?* — and **19 fail it**. The rest already
carry a trigger, several visibly sharpened by the P0 waves since the sweep: **2 of the sweep's own
6 named quotes no longer exist in the corpus at all** ("Prefer semi-synchronous by default",
"Reach for schema-per-tenant only when…" — both since reworded). Rewriting the passing ones would
have been churn on good content.

**Sweep-flagged, left alone, with reasoning** — per the brief's "justify rather than force a reword":

| topic | sweep | why left |
|---|---|---|
| `distributed-locks` | all 7 | every one turns on **efficiency vs correctness**, which *is* the axis of the topic ("Redis only for efficiency; a consensus store plus fencing when the lock is for correctness"). Thin, but flippable |
| `error-propagation` | 2 | all 7 are specific and testable ("the granularity test is *does this code change what someone does?*") |
| `stream-batch-processing` | 2 | all 7 name measurable axes (freshness need, replayability, the measured lateness distribution) |
| `api-design` | 1 | all 7 carry conditions; [3] resolves to "use both" but says which failure each one is for |
| `lambda-organization` | 1 | all 7 are already exceptional (the utilization crossover is computed) |
| `debugging` | 1 | all 7 pair a rule with its named failure mode |
| `observability` | 2nd | [0] assigns a distinct role per pillar; only [3] was circular |
| `state-machine` | 2nd | [1] "event-source when the history itself is the product" is a sharp trigger |
| `retries-timeouts` | 3rd | [1] "propagate a deadline for anything that spans multiple services" is a real rule |

### The 19 rewrites — before and after

**1. `backpressure` [0] Block vs shed under overload**
- *before:* Shed to protect latency for interactive, loss-tolerant, retryable work; block (or spill to disk) to protect completeness for must-not-lose work -- decided by the SLO, often per-priority.
- *after:* Ask what one item is worth *after* the deadline. If it is worthless once late --- a price tick, a search ranking, a presence ping --- shed, because blocking only converts a fast failure into a slow one and you lose the item anyway. If it is worth the same an hour from now --- a payment, an audit row, a customer's upload --- it must not be dropped, so block or spill to disk and let the latency be visible. The giveaway that nobody has made this call is a system that blocks on the interactive path and drops on the durable one, which is exactly backwards.

**2. `circuit-breaker` [0] Trip sensitively vs conservatively** *(sweep-named: "a tautology in the one pane whose job is the axis")*
- *before:* Tune to the dependency's real failure behavior: trip fast enough to protect, with a minimum request volume so noise doesn't flap it. The asymmetry to remember is that a flapping breaker is *worse than no breaker* --- it manufactures degradation on a healthy dependency...
- *after:* Size the window from the dependency's own traffic rather than the library's default, and trip on the **rate**, never the count: a dependency taking ten calls a minute needs a window measured in minutes, because the ten-second default window fills with two calls and one of them failing is a 50% error rate. Then let the asymmetry break every remaining tie --- a flapping breaker is *worse than no breaker*, since it manufactures degradation on a healthy dependency, during normal operation, where no breaker at all would simply have served the request. So when the tuning is genuinely uncertain, trip later.

**3. `event-driven` [0] Synchronous call vs event-driven**
- *before:* Go async where decoupling and burst absorption matter; stay synchronous where the caller needs an immediate result or a transaction across steps.
- *after:* The question is who owns the outcome. If the caller cannot proceed without the answer --- it has to render it, or decide on it --- stay synchronous; making that call async just moves the wait into a polling loop you now also have to build. Go async when the caller's job genuinely ends at *"this happened"*, and use the second-consumer test to check yourself: if you cannot name a consumer you would add later without touching the producer, you may be paying a broker's operational cost to perform a function call.

**4. `feature-flags` [0] Flag behind a deploy vs release with the deploy**
- *before:* Flag anything risky or worth a gradual rollout; ship trivial, low-risk changes directly to avoid flag debt.
- *after:* Flag it when you would want it off **without a deploy** --- so the real question is how fast your rollback already is. If a revert-and-redeploy is fifteen minutes and the blast radius is one internal screen, ship it directly and keep the codebase clean. If the rollback is an hour, or the change touches money, data, or a partner, flag it. And file the removal ticket when you create the flag, because flag debt is not untidiness: a stale flag is an untested code path that is still reachable, and one day something takes it.

**5. `load-balancing` [0] Round-robin vs least-connections**
- *before:* Use round-robin for uniform requests on identical instances; least-connections (or power-of-two-choices at scale) when request costs vary.
- *after:* Look at the *spread* of request cost, not the average. While the p99 sits close to the median every backend gets statistically the same work, and round-robin is free and stateless. Once one request in a hundred costs an order of magnitude more than the rest, round-robin will eventually park several of them on one instance, and the queue on that instance *is* your p99 --- that is the point to switch to least-connections, or to power-of-two-choices once the fleet is large enough that maintaining exact global counts costs more than the balancing buys.

**6. `multi-tenant` [2] Shared schema vs schema per tenant** *(sweep-named: "reach for the middle ground when a middle ground is needed")*
- *before:* Prefer shared rows for scale and reach for schema-per-tenant only when a middle ground between row-scoping and full per-tenant databases is genuinely needed. Be aware of the ceiling: ...
- *after:* The trigger is a per-tenant obligation you cannot express as a predicate on a row: restoring one tenant to its own point in time, migrating one tenant's schema on its own schedule, or handing an auditor a boundary they can see. Scale is never the reason --- shared rows scale strictly further. And the ceiling arrives sooner than people expect: thousands of schemas means thousands of copies of every table in the catalog, which bloats `pg_catalog`, slows planning and autovacuum, and makes a full dump painful. It is a middle ground with a real tenant-count limit, not a free upgrade.

**7. `observability` [3] OpenTelemetry vs a vendor-native agent** *(sweep-named: "circular; contains nothing checkable")*
- *before:* ...Take a vendor agent when its auto-instrumentation coverage and support genuinely outweigh the lock-in for your team's stage.
- *after:* ...Take a vendor agent only where OTel genuinely has no instrumentation for a runtime you actually run, and put a review date on it, because *"their agent is better"* is a claim about this quarter and the decision outlives it. The cost you are weighing is not the licence: it is that re-instrumenting a whole fleet is a multi-quarter project nobody funds, so the lock-in falls due at precisely the moment you have already decided to leave.

**8. `rate-limiting` [1] Local limiter vs shared counter**
- *before:* Use a shared store for correctness, with a local token bucket as a fallback for when the store is unreachable.
- *after:* Count the instances, because that is the error term. Split one limit across N local buckets and a client whose requests land unevenly gets throttled below its quota --- the slop grows with N and with how bursty the client is, and it is invisible to you and obvious to them. So local-only is fine while N is small and the limit is merely protective. The moment the number is **contractual**, or N is large enough that the slop shows up in a customer's own graphs, pay for the shared counter --- and keep the local bucket underneath it as the fail-open path for when that store is unreachable.

**9. `replication` [0] Synchronous vs asynchronous replication** *(sweep-named: "three other panes say quorum commit dominates semi-sync, and this tell is the one the cram sheet ships")*
- *before:* Default to semi-synchronous (sync to one follower, async to the rest) for durability without a single slow node stalling everything.
- *after:* Name the **RPO** first, as an actual number, because that is the thing the business has an opinion about: fully async means a failover loses whatever the replication lag was, so if the answer is *"we cannot lose an acknowledged write"* you are buying a round trip on every commit and you should say what it costs. Semi-synchronous --- one follower sync, the rest async --- is the usual landing spot because it buys two-copy durability for one local round trip. But say the honest caveat: where the data already lives in a quorum store, **quorum commit dominates it**, giving the same durability with no single designated follower whose stall becomes your write latency.

**10. `retries-timeouts` [0] Retry aggressively vs fail fast**
- *before:* Retry a little for transient failures with backoff/jitter/budget; fail fast (circuit-breaker) once a dependency is clearly overloaded or down.
- *after:* Retry only what a retry can fix. A connection reset, a 503, a timeout on an idempotent call --- two or three attempts with backoff and jitter, inside the deadline. A 400, a 404 or a validation failure will fail identically forever, so retrying them is pure amplification against a service that may already be struggling. And the switch to fail-fast is not a judgement call, it is a number: once retries are more than a few percent of your traffic to that dependency, you have stopped recovering from a blip and started **being the load**.

**11. `retries-timeouts` [2] Retry at every layer vs at one layer**
- *before:* Retry at as few layers as possible (often the edge or one designated layer), not at every hop; the amplification of retrying everywhere is catastrophic at scale.
- *after:* Nominate the **one** layer that owns the deadline, and make every layer beneath it fail fast and propagate the code. Retries multiply rather than add: three layers each retrying three times is twenty-seven calls to a dependency that is already in trouble. The layer that should own them is the one that knows the user's deadline, because a retry issued after the deadline has passed is work nobody is waiting for. Write it into the service contract --- everybody else's job is to return a classified error quickly.

**12. `saga` [1] Choreography vs orchestration** *(sweep-named: "owns a far sharper rule two panes away and never uses it here")*
- *before:* Use choreography for simple, few-step sagas where decoupling wins; orchestration for complex sagas with intricate failure handling where explicit, observable flow matters.
- *after:* Choreography for **reactions**, orchestration for **transactions** --- that line is sharper than counting steps, and it is the one to say. Where each service is independently reacting to something that happened, events are right and the decoupling is real. Where there is one business outcome that has to reach a terminal state, somebody must own it, and *"the event graph"* is not an owner: the moment you need to answer *"where is order 12345, and what is it waiting on"*, you need a coordinator with durable state you can query. Compensations are the tell --- if a step has an undo, that flow has an outcome, so it wants an owner.

**13. `sharding-strategies` [2] Co-locate vs global secondary index** *(sweep-named: "the two options are not alternatives, so neither side can lose")*
- *before:* Co-locate by the entity the workload is dominated by (keeping the hot path single-shard), and add a global index (or a CDC-fed search store) for the specific non-key attributes you query often -- accepting its eventual consistency.
- *after:* These are not alternatives, so the decision that actually costs something is *which queries earn a global index* --- and the answer is far fewer than get added. Co-location is chosen once, for the entity that dominates the hot path, and every other access pattern pays a scatter-gather. A global index buys one of them back at the price of a second copy that is eventually consistent and can silently diverge, so add one where the fan-out is **measured** and hurting, never on the suspicion that somebody will want to query by email. The test to say out loud: if you cannot name the query and its rate, you do not need the index yet.

**14. `slos` [0] Higher vs lower SLO target**
- *before:* Set the SLO at the lowest nine that keeps users genuinely happy -- achievable and meaningful, leaving real budget to work with, not aspirational.
- *after:* Start from what you actually measured last quarter, then set the target a notch below it. An SLO you already miss is a permanent freeze, and a permanent freeze gets ignored rather than obeyed; an SLO you beat by two nines has no budget in it, so it never changes a decision --- and a target that never changes a decision is documentation, not an SLO. The number worth defending in the room is the one whose error budget is large enough to spend deliberately and small enough that spending it hurts.

**15. `soft-delete` [1] In-place flag vs archive table**
- *before:* Flag in place for a short reversible window; archive to keep long history without taxing live reads, often purging from soft to archive.
- *after:* The trigger is the **ratio**, not the age. While deleted rows are a small minority, the filter is nearly free and flagging in place is obviously right. Once the dead outnumber the living, every live query is scanning index entries it exists only to discard, and that cost lands on the hot path permanently --- so move history to an archive table and let the archive carry the long retention. The live table's only job is to stay small enough that the common query stays fast.

**16. `soft-delete` [2] Per-query filter vs global enforcement**
- *before:* Enforce the filter globally at one structural point, and make including deleted rows an obvious, explicit escape hatch.
- *after:* There is barely a fork here, and saying so is the senior move: a per-query filter is one forgotten `WHERE` clause away from serving deleted data, and it *will* be forgotten, because the failure is silent and looks like extra rows rather than an error. Put the filter somewhere it cannot be omitted --- a default scope, a view, a row-level policy --- and make including deleted rows a loud, explicit opt-in. The tell that a system got this wrong is a bug report about a deleted record reappearing on one screen out of forty.

**17. `state-machine` [5] Orchestration vs choreography (distributed)**
- *before:* Default to orchestration when the flow has real business logic or an operator must see the state; choreography for simple, high-scale, few-step flows where decoupling wins.
- *after:* Ask whether anyone will ever have to answer *"where is this one, and why is it stuck?"* --- and for anything with a customer attached, they will. That question needs one durable row you can query, which is what an orchestrator is; a chain of events does not have one, so answering it means reconstructing the flow from logs during an incident. Choreograph where the steps really are independent reactions and nobody owns the end state. The threshold in practice is a support ticket: the first time someone asks you to explain a single instance, you needed the orchestrator.

**18. `storage-engines` [0] B-tree vs LSM-tree** *(restates its own bullets: "match the engine to whether reads or writes dominate")*
- *before:* Read-heavy / transactional -> B-tree; write-heavy / high-ingest -> LSM. It's the read-vs-write trade; match the engine to whether reads or writes dominate.
- *after:* The read/write ratio picks the default, but what decides it is the **shape of the reads**. An LSM absorbs writes by never seeking --- append, then compact in the background --- and pays on the read side with a lookup that may touch several SSTables: Bloom filters rescue that for point reads and do nothing for range scans. So high ingest with point lookups is where LSM wins outright, while high ingest with heavy range scans is where the write advantage gets eaten again. And name the cost nobody budgets for: compaction is not free, it competes with your foreground traffic, and an LSM whose compaction cannot keep up degrades into exactly the read amplification you chose it to avoid.

**19. `storage-engines` [2] Row store vs column store**
- *before:* Match layout to access pattern: row store for transactional whole-row access; column store for analytical scan-and-aggregate (and separate OLTP from OLAP rather than forcing one engine to do both).
- *after:* Count the columns the query touches against the columns in the row. Reading three of forty columns across ten million rows means a column store reads under a tenth of the bytes a row store does, and compresses them far better because a column is one type --- that ratio *is* the advantage, so it collapses as the query approaches selecting everything. It also collapses on small result sets, where per-row reassembly across column files dominates, which is why a column store makes a poor transactional database. Split OLTP from OLAP rather than asking one engine to be good at both.

---

## 4. Ratchet arithmetic

| cluster | commit | before | after | class movement |
|---|---|---:|---:|---|
| composer cue-lift + 3 cue rewords | `3f10514` | 66 | 20 | dangling 48 -> 2 |
| when-conj | `dd3c42f` | 20 | 8 | when-conj 12 -> **0** |
| dup + opener dangling | `1fba187` | 8 | 1 | dup 6 -> 1, dangling 2 -> **0** |
| Class E tells | `682d0fc` | 1 | **0** | dup 1 -> **0** |

**Every entry is gone; `test/cram_surface_debt.json` is `{}`.** No entry was left in the baseline,
so there is nothing to justify under "remaining". `0 new defects` and `0 mirror-drift` at every
one of the four steps.

### The mechanism stays
The checker's own doctrine says an empty file means the mechanism *can* be removed. It should not
be — it is now a pure regression guard, and the guard that matters most is the one that survives an
empty baseline. **Verified at this exact end state:** neutering `detectDangling` aborts the run on
**6 self-test fixtures before a browser launches**, exactly as designed for this moment.

### And an empty baseline is NOT "Class H is closed"
The baseline's own §5 says three of the sweep's seven named instances are **semantic** and
invisible to any structural detector — `feature-flags`' Opener saying "instant" when the topic
calls that the junior tell, its whiteboard cues 1-2 carrying a pre-correction framing next to its
own debunk, and `error-propagation`'s Red Flag left un-localized from `notifications`. Those are
untouched by this wave and remain open. The composer fix retired the *readability* half of
`feature-flags`, not the *truth* half.

---

## 5. VR and the gate

**VR: zero churn, as predicted.** `visual_regression` PASS, **16/16 baselines matched their
committed pixels**, no re-capture. Two reasons, both worth recording: the cram sheet is an
overlay that is not captured at rest, and the trade-pane baselines are captured for the flagship
`content-pipeline`, which this wave does not touch at all — no edited string appears in any
captured viewport.

**Gate: 54 checks, 54 PASS, 0 FAIL, 0 SKIP, `GATE: PASS`** — verdict read from the capture file,
not a piped exit code. Run on the freeze tree with `git status` clean; `build_integrity`
independently confirms *"COMMITTED deliverable == fresh build of HEAD"*. The run includes both
tool-wave checks (`cram_surface` and `numbers_lattice`), so this is the merged-tree count.

```
cram_surface        PASS  CRAM SURFACE: PASS  (46 topics, 0 known cram-surface defect(s)
                          allowlisted in cram_surface_debt.json across 0 topics;
                          mirror verified against deriveCram on all 46)
cram_scope_distinct PASS  (46 topics: 46/46 distinct cram bodies, 46/46 distinct scope
                          bodies; shortest 2960 chars)
visual_regression   PASS  (16 baselines, win32-chromium149; ... matched its committed pixels)
```

`cram_scope_distinct` stayed green with the sheets grown, and `ascii_guard` confirms the corpus is
still 7-bit ASCII.

---

## 6. Notes for the cold verifier

1. **The highest-value thing to attack is the two-unit modelling in §1 receipt 2.** If you think
   `dup` should measure the composed line, say so — it would silence `notifications` and
   `rules-engine` and I would have shipped a laundered defect. The numbers are reproducible.
2. **Guard 4 is new and load-bearing.** Revert `_csCueLine` at `cram-derive.js` and rebuild; you
   should get 415 mirror-drift failures. If you get fewer, the guard is weaker than claimed.
3. **The Class E scope is a judgement call and the place I am most likely wrong.** I rewrote 19 of
   the ~36 the sweep flagged, and §3 lists every one I left with reasoning. `distributed-locks`
   (7 tells, all left) is the single biggest disagreement available.
4. **`ascii_guard` does NOT cover the markdown corpus.** It walks `src` but filters to
   `.js/.css/.html` (`test/ascii_guard.py:18`), so every `src/topics-md/*.md` edit in this wave is
   outside it. I verified all 23 byte-by-byte separately; worth an independent check, and arguably
   worth widening the guard in a later wave.
5. Files touched (23 + 6): `src/scripts/app/cram-derive.js`, `src/scripts/app/content-sheet.js`,
   `test/cram_surface.cjs`, `test/cram_surface_debt.json`, **23** files under `src/topics-md/`,
   and `src/topics/{eav,iac,notifications}/trade.js`.

No push, no merge, no `npm install`, no `git stash`.
