# W-X8 "FOCUS HUE" — freeze report

**Wave:** W-X8 (S) · **Finding:** cross-browser audit **X10** (P3)
**Branch:** `xb/x8-focus-hue` · **Base:** `5b94bf6` (gate 70 checks)
**Date:** 2026-07-30 · **Builder:** w20-builder
**Ruling:** team-lead (operator-delegated), `_TEAM_LEAD_W20_BRIEF.md` — the room card's focus
treatment derives from its OWN room, via the hero's exact `color-mix(in srgb, var(--rm,var(--acc)) …)`
pattern; the six-room grid stays a room-enumerating surface.

---

## 1. The ruling's premise, verified before anything was coded

The brief's STOP condition was "if `--rm` is not bound on `.hm-room`, stop and report". It is bound,
structurally and at runtime:

- **Source:** `src/scripts/app/panels.js:282` emits every card as
  `<button class="hm-room" type="button" data-room="<id>" style="--rm:var(--room-<id>)">`.
- **Runtime:** `--rm` resolves to a distinct room hue on all six cards in both themes (§2 table).

The premise holds. No stop condition was triggered. Nothing was improvised.

---

## 2. Before → after, computed, all six rooms, both themes

Measured on the shipped single-file deliverable in Chromium 149 at 1280x800, `#home` route, seeded
theme, per-card `focus({focusVisible:true})` with `:focus-visible = true` asserted on every read.
The two cards the brief asked to be probed by hand are rooms 1 and 2; all six are listed because the
defect was uniform and the fix has to be too.

### 2.1 The defect (pre-fix build)

The **outline** wore the card's own room; the **halo** wore the roomless brand indigo, in every card
and both themes. The neutral is theme-dependent, which the audit's light-only capture did not show —
so this defect had two different wrong colours, not one:

| theme | outline (per card, `--rm`) | halo (all six cards, `--acc`) |
|---|---|---|
| light | the six room hues | `rgb(83,74,183)` @ .15/.20 — `#534AB7` |
| dark  | the six room hues | `rgb(157,147,240)` @ .15/.20 — `#9D93F0` |

Card 1 (messaging-events), light, verbatim:

```
outline    : 2px solid rgb(0, 107, 99) off=2px
box-shadow : color(srgb 0.32549 0.290196 0.717647 / 0.15) 0px 0px 0px 3px,
             color(srgb 0.32549 0.290196 0.717647 / 0.2)  0px 0px 16px -4px
--rm on the element rgb(0,107,99)      --acc on the element rgb(83,74,183)
```

Reproduced by **real `Tab` presses** from a cold home as well as programmatically — tab 1 lands on
messaging-events, tab 2 on data-storage, both showing the same split.

### 2.2 A second half of X10 the audit did not record

`box-shadow` is **one property**. `.hm-room` carries `box-shadow:inset 3px 0 0 var(--rm)` at rest
(`styles.css:2026`) — the 3px room stripe down the card's left edge. Because the generic
`button:focus-visible` supplied the whole property on focus, **the stripe was not overridden in
colour, it was deleted**:

```
REST    box-shadow : rgb(0, 107, 99) 3px 0px 0px 0px inset
FOCUSED box-shadow : <the two neutral glow layers>          <- no inset layer at all
```

Measured `inset=[]` on all six cards in both themes. So focusing a room card did two wrong things:
painted the wrong hue **and** removed the card's clearest room marker. X10 records only the first.

### 2.3 After the fix

All three parts now derive from the card's own `--rm`:

| # | room | theme | outline / inset stripe | halo (15% / 20% of `--rm`) |
|---|---|---|---|---|
| 1 | messaging-events | light | `rgb(0,107,99)` | `color(srgb 0 0.419608 0.388235 / .15 / .2)` |
| 2 | data-storage | light | `rgb(49,91,180)` | `color(srgb 0.192157 0.356863 0.705882 / …)` |
| 3 | reliability-observability | light | `rgb(146,78,0)` | `color(srgb 0.572549 0.305882 0 / …)` |
| 4 | platform-infra | light | `rgb(105,78,176)` | `color(srgb 0.411765 0.305882 0.690196 / …)` |
| 5 | architecture-apis | light | `rgb(150,61,134)` | `color(srgb 0.588235 0.239216 0.52549 / …)` |
| 6 | security-tenancy | light | `rgb(167,58,87)` | `color(srgb 0.654902 0.227451 0.341176 / …)` |
| 1 | messaging-events | dark | `rgb(19,186,172)` | `color(srgb 0.0745098 0.729412 0.67451 / …)` |
| 2 | data-storage | dark | `rgb(125,166,243)` | `color(srgb 0.490196 0.65098 0.952941 / …)` |
| 3 | reliability-observability | dark | `rgb(225,149,86)` | `color(srgb 0.882353 0.584314 0.337255 / …)` |
| 4 | platform-infra | dark | `rgb(173,154,238)` | `color(srgb 0.678431 0.603922 0.933333 / …)` |
| 5 | architecture-apis | dark | `rgb(218,141,202)` | `color(srgb 0.854902 0.552941 0.792157 / …)` |
| 6 | security-tenancy | dark | `rgb(238,140,164)` | `color(srgb 0.933333 0.54902 0.643137 / …)` |

Card 1, light, verbatim after:

```
outline    : 2px solid rgb(0, 107, 99) off=2px
box-shadow : rgb(0, 107, 99) 3px 0px 0px 0px inset,
             color(srgb 0 0.419608 0.388235 / 0.15) 0px 0px 0px 3px,
             color(srgb 0 0.419608 0.388235 / 0.2)  0px 0px 16px -4px
```

Re-confirmed by real `Tab` presses on the fixed build for cards 1 and 2.

---

## 3. What changed

**`src/styles.css:2028-2056`** — one rule, plus the note explaining it:

```css
.hm-room:focus-visible{outline:2px solid var(--rm);outline-offset:2px;
  box-shadow:inset 3px 0 0 var(--rm),
    0 0 0 3px color-mix(in srgb,var(--rm,var(--acc)) 15%,transparent),
    0 0 16px -4px color-mix(in srgb,var(--rm,var(--acc)) 20%,transparent)}
```

- **Same alpha steps and mechanism as the hero** at `:2007` — 15%/20%, `color-mix(in srgb …)`, the
  same mixing the `--acc-a*` rungs use at `:116`.
- **Same fallback discipline** — `var(--rm,var(--acc))`, which is what keeps `phantom_tokens.py`
  honest about a token no stylesheet declares (it is set by inline style; the guard's own self-test
  covers exactly this shape and is green: 3 phantoms, 3 allowlisted, 0 NEW, 0 STALE).
- **The outline is untouched.** It was already `2px solid var(--rm)` — the card's own room — so
  outline and halo now agree. The hero's precedent (leave an `--ink` outline alone) did not apply
  here; there was nothing to leave alone.
- **The inset stripe is re-declared.** This is the one place the build goes past the letter of the
  ruling, and deliberately: once this rule declares `box-shadow`, the omission of the stripe becomes
  *this rule's* choice rather than the cascade's. Restoring it serves the ruling's own principle —
  the card's focus treatment derives from its own room — and dropping it would have shipped a
  "wears its own room" fix that deletes the card's room stripe. `.hm-cta:2007` carries no inset
  because the hero has nothing to preserve (its rest shadow is a drop-glow under a `--rm`-filled
  button). **Flagged here explicitly for the verifier as the one judgement call in the wave.**
- **The global rule at `:53` is untouched**, and `.hm-cta` is untouched.
- **The six-room grid is untouched** — it remains a room-enumerating surface under the
  `CROSS-GROUP SURFACES ARE ROOMLESS` neutralisation at `:1502`. Only each card's own ring speaks
  for its own room. The ruling's boundary is respected exactly.

---

## 4. Contrast: stated, not invented

**The halo is decorative and no contrast gate is added.** WCAG 2.4.7 focus visibility on this
element is carried by `outline:2px solid var(--rm)` with `outline-offset:2px`, which is **unchanged
by this wave** — same width, same style, same colour, same offset as on the pre-fix build. The halo
is a 15%/20%-alpha glow layered on top; it adds emphasis and cannot subtract from the outline.
No contrast threshold moves, so inventing a numeric gate here would be a check with nothing to
measure. `room_contrast.py` continues to own the token side of the palette.

---

## 5. The guard — `test/focus_ring.cjs`, 6 arms → 12

The audit was explicit (§8): *"`focus_ring.cjs` never reads `box-shadow`. All three of its probe
returns are `{fv, width, style, color, offset, accRgb}` — outline only. It structurally cannot see
X10, and `.hm-room` is not among its selectors."* Both halves are now closed.

**No new check file** — the wave extends the existing one, so the gate stays at **70 checks** and
`focus_ring` goes from **6 assertions to 12**.

### What the new arms ask

Six new assertions (three questions x two themes), on the `#home` route in fresh isolated contexts:

| arms | assertion |
|---|---|
| 7-8 | `.hm-cta` halo derives from the room it will OPEN — a **regression guard on W15's fix**, which nothing guarded before |
| 9-10 | all six `.hm-room` halos derive from the **card's own** `--rm`, not the roomless `--acc` — **X10** |
| 11-12 | a focused `.hm-room` **keeps its inset room stripe** — the §2.2 half |

**Hex-free, probe-resolved (the W15 pattern).** There is not one colour literal in the arms. A hidden
probe span is appended *inside* the element under test and given the expected declaration through the
**same property** (`box-shadow`), then its computed value is compared to the element's own — same
inherited `--rm`, same engine, same serialisation path. A hardcoded hex here would be a second copy
of the palette that drifts the day a room is retuned.

**It carries its own negative control, every invocation.** The neutral halo the generic rule would
paint is resolved the same way (`var(--acc-a15)`/`var(--acc-a20)` off the same element) and the arm
asserts the halo is **not** that. It also asserts `--rm` and `--acc` genuinely **differ** on the
element and **fails rather than passes** if they do not — otherwise a room whose hue happened to
equal the neutral would green the check for free. That is not theoretical: platform-infra is
`rgb(105,78,176)` against `rgb(83,74,183)` in light and `rgb(173,154,238)` against `rgb(157,147,240)`
in dark, which is also precisely why this defect could sit in a screenshot unnoticed. The arm also
fails closed if the probe itself computes to `none`.

### Watched RED — capture: `_audit/2026-07-30-w20-hue-watched-red.txt`

Run against the **pre-fix** deliverable (src edited, deliverable not yet rebuilt), exit 1:

```
FAIL  [light] all six .hm-room focus halos derive from the card's OWN room ... -- audit X10
FAIL  [light] a focused .hm-room KEEPS its inset room stripe ...
FAIL  [dark]  all six .hm-room focus halos derive from the card's OWN room ... -- audit X10
FAIL  [dark]  a focused .hm-room KEEPS its inset room stripe ...
FOCUS RING: FAIL (4)
```

The failure detail is the proof, not just the verdict: for every one of the twelve card-theme pairs
the measured halo is **byte-identical to the "the roomless `--acc` would paint" column**, and
`inset=[]` on all of them. The four original arms and the two `.hm-cta` arms stayed green on that
same run — the red is scoped to exactly what the wave fixes.

### Mutants — capture: `_audit/2026-07-30-w20-hue-mutants.txt`

The `.hm-cta` arms are green on the pre-fix build by construction (W15 already fixed the hero), so
watched-red does not demonstrate them. They were proven separately: on the **fixed** build, the
`box-shadow` declaration is deleted from the rule under test via CSSOM — which is exactly that rule
reverted — and the verdict is re-measured.

```
MUTANT .hm-cta  -- removed box-shadow from `.hm-cta:focus-visible`  (1 rule hit)
  BEFORE halo verdict = PASS   halo [color(srgb 0 0.419608 0.388235 / .15) | … / .2]
  AFTER  halo verdict = FAIL   halo [color(srgb 0.32549 0.290196 0.717647 / .15) | … / .2]
  => FLIPS PASS -> FAIL

MUTANT .hm-room -- removed box-shadow from `.hm-room:focus-visible` (1 rule hit)
  BEFORE halo PASS / stripe PASS
  AFTER  halo FAIL / stripe FAIL   (inset=[] , want [rgb(0, 107, 99)])
  => FLIPS PASS -> FAIL
```

Both arms can fail. (The mutant harness needed one correction worth recording: the app ships its CSS
inside a `@layer` block, so the rules sit under a `CSSLayerBlockRule` and a flat walk of
`sheet.cssRules` finds **nothing** — the first run reported `0 rule(s) hit` and both mutants
"did not flip". That was the harness lying, not the check. Recursing through grouping rules fixed it.
This is the repo's own lesson again: every fresh instrument is buggy on its first attempt, so a
mutant that reports "no change" must be suspected before the check is.)

### Green on the fixed build

```
FOCUS RING: PASS  (12 assertions: 3 light-DOM chrome buttons kept their ring; the shadow #adv, #jg
and .piv-jump get the BASE_SHEET ring; and in both themes the .hm-cta and all six .hm-room focus
HALOS derive from their own room, each against a live negative control)
```

---

## 6. VR — 16/16 byte-identical, as predicted

The audit predicted **NEUTRAL**: baselines capture at rest and `.hm-room` is never focused in
`home-light`/`home-dark`. Confirmed as a hard check, not an assumption — see the gate capture:
`visual_regression` reports **16 baselines, win32-chromium149, matched its committed pixels**, with
no re-baseline and no tolerance spend. The hero's autofocus ring *is* in those two baselines and is
unchanged, which is the sharper reason the run is meaningful: `.hm-cta` was deliberately not touched.

---

## 7. Gate — capture: `_audit/2026-07-30-w20-hue-gate.txt`

Commit-first-then-gate: the run of record is on the **committed tree**. **70/70 PASS**, `focus_ring`
carrying 12 assertions instead of 6.

### 7.1 The first gate run went red, and the check was right

Worth recording, because the failure was in *documentation* and it is a trap anyone writing a
before/after note in this repo will hit:

```
room_static  FAIL  - 1 hardcoded indigo rgba literal(s) regrew -- use an --acc-aNN rung
```

`test/room_static.py:23` walks `src/**` and greps raw text for `83,74,183|109,95,214` so that the
226-literal accent codemod cannot regrow. My CSS comment had documented the measured defect as
`halo rgb(83,74,183)` — the exact digits the guard hunts. Rule 1 does not skip comments (rule 2, the
infinite-animation one, explicitly does, so the file's author knew the distinction and did not draw
it here).

**Fixed in the prose, not in the check.** The comment now says `#534AB7`, which is also the repo's
own convention for this colour three lines away at `:2002` ("a #534AB7 ring hugging a #006B63
fill"). Teaching `room_static` to skip comments would have weakened a live guard to accommodate a
comment I could simply write differently — the wrong trade, and out of a focus-hue wave's scope.
The same substitution was made in `focus_ring.cjs`'s note; `test/` is outside that walk today, but
writing the retired indigo as digits anywhere is a tripwire waiting for the day the walk widens.

No other check moved, and the fix is comment-only: the `.hm-room:focus-visible` declaration is
byte-identical across the red and green runs.

## 8. Files

| file | change |
|---|---|
| `src/styles.css` | `.hm-room:focus-visible` — the room-derived halo + the re-declared inset stripe, with its note (`:2028-2056`) |
| `test/focus_ring.cjs` | arms 7-12: the `box-shadow` half of a focus indicator, hex-free, with a live negative control |
| `deepdive_content_pipeline_rehearsal.html` | rebuilt deliverable |
| `_audit/2026-07-30-w20-hue-watched-red.txt` | pre-fix red capture |
| `_audit/2026-07-30-w20-hue-mutants.txt` | mutant proof both arms can fail |
| `_audit/2026-07-30-w20-hue-gate.txt` | committed-tree gate run of record |

## 9. For the verifier

1. **The one judgement call is the inset stripe** (§2.2, §3). The ruling authorised the halo; the
   stripe restoration is mine. The argument is that declaring `box-shadow` here makes the stripe's
   absence this rule's own choice, and that deleting a card's room stripe contradicts the ruling it
   would be shipping under. If the verifier disagrees, removing `inset 3px 0 0 var(--rm),` from
   `:2054` and arms 11-12 backs it out cleanly; nothing else depends on it.
2. **Untouched on purpose:** `button:focus-visible` (`:53`), `.hm-cta:focus-visible` (`:2007`), the
   `.hm-room` outline, the `:1502` neutralisation, and the six-room grid.
3. **No master contact.** Nothing merged, nothing pushed.
