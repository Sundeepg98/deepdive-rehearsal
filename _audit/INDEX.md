# deepdive-rehearsal — audit index

- [2026-07-11 — COMPILER PARITY FIX, independently verified](2026-07-11-compiler-parity-fix.md) — a skeptic's re-verification of the claim that the compiler was made lossless. **The thesis is confirmed: the depth gap was an ENGINEERING failure, and the fix is real.** Root cause, definitively: `TOPIC_MARKDOWN_FORMAT.md:238` calls System stages a bullet list while its **own worked example at `:251-253` writes them as plain lines** — all 38 authors copied the example, markdown-it merged the adjacent lines into one paragraph, and the first matching field swallowed the rest. **3 of the spec's 12 worked examples did not survive the parser that documents them.** Re-derived from scratch with instruments importing none of the fixer's tooling: **+608 items recovered by engineering alone** (189 sys stages, 114 tier notes, 76 pivot answers, 76 beat models, 76 interviewer questions, 38 stage-cursors, 38 curveball themes), parity **26% → 36%** on the prior audit's metric (**17.9% → 23.2%** counting every slice the 8 have). Browser-verified on all 46: **System Maps 0/38 empty (was 38/38), 189 stages painted; literal `undefined` gone (0 of 3 sites); 46/46 cross-topic jumps land correctly; 0 console errors; THE 8 byte-identical and intact.** **The gate is no longer self-confirming — proved by restoring the old parser and re-running it: the new gate FAILS (conservation, doc-examples, compiler_md 7-fail, build_integrity) on the exact parser the old gate passed 19/19.** The remaining depth gap is **genuinely authoring** — `Follow:`/`Senior:`/`Speak:` appear **0 times in all 38 `.md` files** vs 173 each in the 8, and a format-ceiling probe proves the markdown *can* express them (`FORMAT CEILING: none`). **⚠ ONE LIVE DEFECT REMAINS, and it is engineering, not authoring: the coaching rail still shows ANOTHER TOPIC'S note on 64% of pane-topic combos** (266/414) — `shell.js:237` `if (TOPIC_CMP_NOTES[tab]) {…}` has **no `else`**, so a topic with no note for the active pane keeps the previous topic's note on screen; caught cold in the shipped build (on `caching`, the System Map rail serves **IaC's** note). Not fixed, not caught by the gate. Evidence: `_audit/2026-07-11-compiler-parity/` (5 verifier-authored instruments + proof shots).
- [2026-07-11 — FULL STATE AUDIT (16 lenses, adversarially verified)](2026-07-11-full-state-audit.md) — the system of record for the whole app. 16 lenses (5 inventory / 8 runtime-Playwright / 3 visual-design), each re-verified by a second agent that tried to refute it; refuted claims stripped. **Verdict: a fundamentally sound app with a broken build-time content path and four one-line show-stoppers — nothing needs a rewrite, almost nothing needs cutting.** Headlines: (1) **the app renders a BLANK PAGE under `prefers-reduced-motion`** — `styles.css:90` sets `body{opacity:0}` and `:137`'s `animation:none!important` deletes the reveal; 4 lenses caught it, the a11y lens *certified it passing*; 1-line fix. (2) **On a phone the Tools button — the ONLY entry to 12 tools — is off-screen on 16–24/46 topics and unreachable at ANY point on the display on the longest topic even at 430px**; one missing `min-width:0` (`styles.css:825`); 1-line fix. (3) **46 topics = 38 markdown + 8 legacy JS**, and the 38 are **28% of reference depth** — because the compiler **silently discards 189 authored System-Map stages, 114 tier notes and 76 pivot answers on every build** (`parse_md.mjs` needs bullet lists; authors used the plain-line form *its own docs show*) → empty System Maps, `undefined` on screen at 3 sites, 2,100px chips clipped mid-word, dead cross-topic jumps, mis-navigating jump buttons. A verified shape-only reshape recovers all ~380 items with **zero compiler changes**. (4) The **Cram sheet + Scope overlay serve Content Pipeline's content on 45/46 topics** under a correct header. (5) The app's own **"Drill my 3 Revisit probes →" button destroys a completed 22/22 run** (`{done:22,tot:22}` → `{done:1,tot:3,revisit:[]}`). (6) The **visual trainer draws nothing (0×0 canvas)** but is **2 × S-effort bugs from working** — sim 11/11, pipeline correct, pilot renders; its only working configuration is reduced-motion, i.e. the blank-page one. (7) **The 5.1MB is NOT a problem** — stripping 25% of the bytes made boot *slower*; boot is layout-bound. **Root cause of everything: the gate tests truthiness, not population (`if (!data[v])` — `{stages:[]}` is truthy); every defect shipped through a green 19/19.** 21 findings P0→P3, full 46-topic inventory table, prioritized visual plan, decisive KEEP/DROP (**DROP all 20 non-master branches + 3 of 4 archive tags; DROP zero topics; zero orphaned modules — the rot is in DATA, not code**), 4 strategic options + recommendation (Wave 0 one-liners → harden the gate → compiler fix → content & visual tracks in parallel). Evidence: `_audit/2026-07-11-state-audit/` (16 lens + 16 verify reports, ~120 scripts, ~400 shots).
- [2026-06-29 — Keystone A: Topic Conductor Build-Ready Design](2026-06-29-keystone-a-design.md) — the full build plan for the per-topic DATA contract (the gap-analysis's Keystone A). **Mechanism**: `TopicPane` base class + `TopicRegistry` singleton + one `deeptopicchange` event; `setTopic(id)` reseeds cross-pane globals + rewrites scattered identity, fires ONE event, every pane re-invokes `renderTopic(slice)` IN PLACE (host identity / shadowRoot / adopted sheets / listeners all preserved — uniformity of a single switch without recreating elements; mixin rejected). Covers: best-of-both protocol + why; canonical base API (topic-protocol.js, full code) + init/renderTopic lifetime split; full per-topic schema (+ wb.sub/okVerdict, sys.heads, num.compute extensions); disjoint-file layout (per-pane ownership table); setTopic ordered sequence + router topic-axis + identity-binder; per-pane table + all 9 specs; foundation-first build order (F1–F6 → 9 parallel worktree panes → topic-2 proof) + hard-pane analysis (drill L / wb M by LOC; trade/rf/open/num by novelty). **8 honestly-flagged gaps** incl. abbreviated schema, non-uniform foundation aliasing (model forward-ref + 3 fully-baked no-alias), the shared unit_tests.py manifest merge-conflict risk, and topic-2 = real authoring.
- [2026-06-29 — e2e / Playwright Verification Layer](2026-06-29-e2e-verification-layer.md) — closes the #1 documented risk: THE GATE now RUNS browser checks instead of skipping everywhere. Portable Chromium detection (`playwright.chromium.executablePath()`, no hardcoded /opt + NODE_PATH), package.json devDep, CI installs playwright+chromium before the gate, new `test/e2e_interactions.cjs` (theme/text-zoom/must-hit/rescues, 0 console errors), + a render.cjs flakiness fix the running-vs-skipping exposed (fixed 70ms wait -> deterministic waitForFunction). master `2bef4a5`->`ebd7203`, test-infra only (deliverable byte-unchanged). **CI run 28371382740 success 53s — browser checks ran green in CI for the first time.** Honest soft spots: scroll-to-top soft assert + page-visibility wiring-only (headless can't force document.hidden; full path hand-verified).
- [2026-06-29 — Batch 1 Build Verification (rescues + must-hit-points)](2026-06-29-batch1-verification.md) — the operator's "Quick rescues + must-hit-points" batch, built in 2 worktree-isolated agents, browser-verified (Playwright, light+dark, 0 console errors), serial-merged GATE-gated, CI-deployed live. **5 features**: must-hit-points grounded drill self-scoring (`ad0bec1` — per-probe `<b>`-term checklist from each card's own answer+senior, coverage-driven Solid/Revisit rec, debrief names the dropped phrase; CPR1/getStats untouched) + 4 offline-safe rescues (`12a5b2b` — text-zoom A-/A+, pomodoro 25/5, page-visibility anim-pause, scroll-to-top). master: `e93ed7b`->`ad0bec1`->`12a5b2b`. Includes the disjoint-source/regenerated-deliverable merge recipe for multi-worktree builds. Branch cleanup (#9) now UNBLOCKED.
- [2026-06-29 — Tiered Reconsideration of Dropped/Deferred Features](2026-06-29-reconsideration.md) — all 77 previously-dropped/deferred items re-scored under the operator's "implementation ≠ value" lens (offline-unsafety is a fixable impl detail, not a reason to drop a useful feature). **12 RESCUE_WORTHY, 47 clean drops, 18 operator-taste.** Key structural finding: the 12 rescues are *exactly* the 12 Tier-2 (fixable-env) items — the lens maps precisely onto Tier-2; nothing useful is trapped in Tier-1/0/3. Strategic miss = the real-browser **Playwright e2e/verification layer** (#1 documented risk; effort L) + 4 S-effort UX/a11y singles (font-size A-/A+, scroll-to-top, pomodoro, page-visibility) + a 4-line MOBILE-SPACING test guard. Standout false-positive: `pomodoro-timer` dropped on a phantom `sessionStorage` tag (its code uses no storage). Bonus master-hygiene 1-liner: dead `animation:activePulse` in `model-answers/logic.js:15`. Supersedes the earlier 3-chunk/58-item draft.
- [2026-06-29 — Forward Gap-Analysis Roadmap](2026-06-29-gap-analysis.md) — what's genuinely MISSING (not branch salvage), synthesizing the 5 per-dimension ideation passes. Two-shaped gap: **BREADTH** (ships "Topic 1 of 8" but only 1 exists; the other 7 are named + bridge-authored in `SYS_PIVOTS` but unbuilt — content-XL) and **DEPTH-OF-MEMORY** (CPR1 is a lossy aggregate scoreboard, not a per-signal save-file). **Synthesis verdict: 5 dimensions collapse onto 2 keystones + 1 honesty substrate** — Keystone A = the per-topic DATA contract (data-ize the 4 HTML-baked surfaces + registry/router/switcher, L) unlocks all curriculum; Keystone B = the CPR2 per-signal mastery code (L) unlocks the retention half of learning-science AND the heatmap/save-game half of persistence (same upgrade, two dimensions); the must-hit-points checklist (M) grounds every self-score so CPR2 stores honest data not vibes. Ranked top-7, 5 dimension sections, 3-phase build order + orthogonal a11y track, full 36-feature table, honest XL caveats.
- [2026-06-29 — Branch Merge-Triage](2026-06-29-branch-audit.md) — all 6 branches triaged in isolated worktrees + adversarial verify (13 agents). Verdicts: `enhance-web-components` **MERGE** (fixes latent unstyled-companion bug); `visual-enhancements` **SALVAGE** (visual track + the real guard tests master's RCA over-claims); the two giants v300/v308 = **one bloated lineage** (~88% generated JSON/PNG + offline-violating network code) → file-extract the ~1.2K-LOC SPA core only, never merge; `gh-pages` **REDEPLOY** (serving a stale unmerged commit); `master` keystone fix = `.gitattributes` EOL normalization. Recommended serial plan in the report (Step 0 foundation → enhance-web-components → visual-enhancements → SPA scope gate → SPA extract → gh-pages redeploy).
- [2026-07-13 — Room-accent system VERIFIED ALREADY SHIPPED](2026-07-13-room-accent-verification.md) — dispatched to *build* accent threading off a stale brief (the 07-11 audit's "`--topic-accent` is never set / 7px dot" line); the agent rendered pixels first and proved the six-rooms system **already fully built, contrast-solved and test-guarded** (`--topic-ink/solid/wash/edge → --acc*` at styles.css:177-203, `applyIdentity` `data-group` stamp, ~690 consumer sites in 23 shadow sheets, room_contrast + room_contract + pixel baselines). Records the raspberry-not-red security rationale and the **deliberately-neutral surfaces** so nobody "fixes" the restraint. 12-shot evidence matrix in `shots/2026-07-13-rooms/` (untracked). Branch `vis/v-accent` closed as a verified no-op. **Do not brief visual work from the 07-11 audit.**
- [2026-07-13 — Visual sweep punch list (8 lenses, deduped, packaged)](2026-07-13-visual-sweep.md) — **system of record** for the 8-lens visual sweep, deduped and cut into 4 collision-free work packages along ownership boundaries. **13 actionable: P0=0, P1=1, P2=7, P3=5.** Dominant theme is **pane-content typography** — reading prose renders 12–13px (below the 14px floor) and runs 100–129ch at 1536px, all *inside the nine shadow-DOM panes `styles.css` cannot reach* (fix lives in `base-styles.js`/`shared-sheets.js`/pane modules, never `styles.css`). Lone **P1** is a recoverable routing dead-end — browser Back from the landing index → **blank white page**, independently **CONFIRMED** (0 non-white px at both viewports) though the verifier corrected the mechanism (it is `replaceState`-to-prior-document, not a push/router-no-op; `router.js:174` already replaceStates the boot hash). Packages: **PKG-A** app-shell JS routing (`router.js`, dispatch first) · **PKG-B** per-pane shadow CSS typography+measure · **PKG-C** app-shell JS group-colour single-sourcing (`groups.js` `TOPIC_GROUPS[].color` is dual-sourced vs CSS `--room`; same-group two-hue, dark 3.37:1) + index-overlay Cram pills · **PKG-D** global `styles.css` mobile tab-strip affordance (3/9 tabs @360) + 500ms motion budget. **`--topic-accent` confirmed a dead zero-consumer token — do NOT thread it; the real seam is `TOPIC_GROUPS[].color`→`var(--room-*)`.** ⚠️ Two lenses (`mobile-tools`, `overlays-desktop`) returned placeholder stubs → **desktop dialog overlays and mobile Tools are UNCOVERED, re-run before closing.** Evidence: `_worktrees/deepdive-rehearsal/v-sweep/_sweep-shots/` (536 shots).
- [2026-07-13 — THE VISUAL WAVE: 7/7 merged, gate 40/40](2026-07-13-visual-wave-shipped.md) — the punch-list fix wave's system of record: seven independently-verified merges (build determinism + guard; click-surface 55.6px→0 + guard; cta_contrast paint-settle repair; colour single-sourcing with the second palette DELETED; the shadow-pane typography overhaul; Back-navigation fix + guard; mobile tab-strip affordance + the load-bearing pin commented AND asserted). Every instrument negative-control-proven; one bounce-for-fix kept the wave asterisk-free; mobile drift certified negative by dual instruments. Full evidence map + deferred-nits list inside. **NOT PUSHED — operator decisions pending: push + homepage.**
- [2026-07-18 — "Manually constrained, no smoothness": the 5-lens root-cause synthesis](2026-07-18-manual-smoothness-analysis.md) — the operator's felt verdict, run to ground across 5 driven lenses (click-ledger, motion-map, jank-trace, input-grammar, flow-critique), load-bearing findings adversarially re-verified. **Verdict: the feeling is real and literal, but it is NOT missing capability — it is a wiring/surfacing/application problem; every lens independently found the smooth/guided version already ships and is simply not applied to the default path.** Four ranked root causes: **(RC-A, dominant, architecture)** the app is a control panel not a path — **62 of 63** journey actions are hand-driven (the whole product auto-advances in **exactly 1** place), **0 of 4** completion surfaces hand forward, **0** "continue/next" controls doc-wide, the real `pickRec()` next-move engine buried **314px below the fold** behind a modal, ~**64** controls/screen with ~**1** recommendation; **(RC-B, dominant for "no smoothness", deliberate-trade + application gap)** **~50%** of state-change types (8/16) are motion-dead hard snaps on the **highest-traffic** surfaces with `transition-duration:0s` on `#home`/pane-host/all 7 overlays and a **~111ms** un-animated swap per click — the `pop` reveal animation silently no-ops (0 `@starting-style`), HOME→index is byte-identical at +40ms vs +540ms; **(RC-C, input-modality gap, amplifier)** **22/22** keyboard shortcuts WORK but **0** hints on the 9 tabs + **0** `aria-keyshortcuts` → ~100% pointer-driven on **21/49** sub-44px targets, mobile swipe dead-ends at the 2 diagram panes; **(RC-D, performance, latent)** fast machine barely hitches (**668ms** blocked/30-interactions, 24/30 zero long tasks) but mid-range 4×-CPU explodes **21×** to **14,008ms** blocked / 8.4s boot / 2.5s frozen input. **Candid on the wave:** it correctly stripped transforms off hit surfaces (click-surface 55.6px→0) and removed `startViewTransition` (ate 150–500ms on the 11.6MB doc) — but hit-test-safe opacity/crossfade motion still ships on the overlays and was simply never re-wired onto the default path, so **most of RC-B is re-wiring debt, not a tradeoff that must stand**; the SVG bake made perf net-positive. Fix map = opacity-only quick wins (crossfade the default path, fix the `pop` no-op, surface the keys, persist sub-position, default resume ON) + structural spine (a "Continue" affordance extending the drill's proven chain, completion-as-hand-off via `pickRec`, weakness-ranked sequencing) — each tagged with root cause and the three invariants it must not break (click-surface stability, reduced-motion, offline single-file). Adversarial re-verify walked back RC-D's tail (circuit-breaker **~1.2–1.4s** not 2.4s; wb is among the LIGHTEST not heaviest) and confirmed RC-A/B/C exactly. Sharpest tell: the opener's `[drill]/[walk]/[trade]` chips imply cross-pane passage but are **decorative (0 interactive)** — the connective tissue is painted on, not wired. Evidence: `shots/2026-07-18-manual-analysis/` (68 shots).
- [2026-07-18 — Flow design judge-panel: synthesis & operator decision](2026-07-18-flow-design-panel.md) — 5 independent flow designs (continuity / session-arc / minimal-touch / homepage-led / data-led) scored by 3 citation-verifying judges. **Scoreboard: D3 minimal-touch 131 > D1 Continue-Spine 129 > D4 front-door 124 > D5 evidence-ladder 122.5 > D2 practice-session 107.5 — and all three judges independently prescribed the same composite: D3's hand-offs first, D1's spine as the destination.** Three synthesized directions — **A (RECOMMENDED): staged "close the loop, then run the spine"** (W0 un-lie `mock.last` + pickRec 6.5 + microtask freshness law → W1 = five terminal hand-offs w/ receipts ≈5d → W2 = NextUp dock + one-compute contract + `pos.<id>` cursor resume ≈5-6d; committed as a PAIR); **B: the spine outright** (same end-state, one ≈9-10d train, unseverable mobile bet); **C: front-door-led** (home TODAY plan + stagehead chip, no persistent chrome, lobby-first forever). Binding adversarial facts: there are FIVE+ terminal surfaces (TWO mock ends — `#vrestart` drill/logic.js:802-806 AND `#mbagain` mixed-fire.js:218-257; no design covered both); `mock.last` is a global topic-less lie (re-key before promoting mock recs); `_updCount` fires before `_emitGraded` → all completion recs compute on queueMicrotask; pickRec never hands anyone to a FIRST mixed fire (branch 6.5 fixes). **The homepage question is CLOSED: standalone `#home` already shipped, overlay stays the `\` switcher — the only variable is the home's job.** 12 anti-goals (no auto-cross, no second decision engine, no unreceipted guidance, no gamification, SELF-dedupe, gate-or-don't-ship). Raw designs + judge texts: workflow journal.
- [2026-07-18 — Mid-range (4x CPU) perf track: attribution → chunking prototype → cold verdict](2026-07-18-midrange-perf.md) — RC-D run to ground. **Attribution inverted the blame:** all 10 panes DO re-render synchronously per topic entry, but that script is ~6–9% of the entry task — the money is 3–4 forced full-document style+layout passes per entry (`upd()` `void offsetWidth` ×2, `_maybeRender` `offsetParent`, `pinTop`/`scrollTop`; ~450–650ms waste @4x), hidden panes' layout bills deferred to first reveal (105–521ms/switch), and the 11.6MB boot parse floor (~6.2s @4x); plus a measurement trap (the `longtask` API silently drops the big throttled tasks — trust trace `RunTask`). **Prototype** (`TopicPaneQueue`, candidate #2: active-pane-first + one-pane-per-macrotask drain + flush-before-reveal) cold-verified **PARTIAL**: real **−45% input→paint @4x with a heavy pane active, −20..−53% heavy-topic entries**, 1x strictly safe, both gates PASS — but the "855–1185ms entry task" was load-inflated (calm base 96–422ms; true saving ~100–130ms, exactly attribution's own band), work is redistributed not reduced, and **one reproduced regression: `_tpOnScreen` uses the hash as visibility oracle, so after the session panel's "Back to the drill" (default rec) every topic change paints byte-stale content on the VISIBLE pane** (390ms @4x) — violates the prototype's own invariant. **Recommendation: ITERATE** (fix the oracle from `.pane.on`, re-verify, restate the win honestly) — NOT merged; prototype lives uncommitted in worktree `perf-chunk`. Forced-reflow batching (predicted −450..−700ms/entry) is the bigger, untouched next track.
- [2026-07-18 — Smoothness quick-wins wave (QW1–3): 3/3 CLEAN, merge-ready](2026-07-18-smoothness-quickwins.md) — the three opacity-only quick wins from the manual-smoothness fix map, built off base `54596e7`, each cold-verified in **1 round** with negative controls on base; **nothing bounced**. **QW1 `qw/crossfade` `93dfae6`**: the 3 genuinely dead swaps (topic-body, `#home` enter, dropdown open) now fade 150ms opacity-only — root cause: `setTopic` repaints shadow panes IN PLACE (`.on` never re-toggles so `panein` never restarts) + two bare display flips; pane switch byte-identical, no transforms, no `startViewTransition`; gate 40/40, VR 16/16 worst 0 px, zero re-captures. **QW2 `qw/pop` `c52df50` — marquee root cause: `@keyframes` are TREE-SCOPED in shadow DOM.** 11 rules across 5 shadow scopes referenced `pop`; only the drill's scope defined it — 4/5 scopes computed `animation-name:pop` yet instantiated NOTHING (0 events, `getAnimations()`=[]); the document-level copy can't reach shadow. **Corrects RC-B's `@starting-style` hypothesis (pop is an animation, not a transition — measured and disproven).** Fix = define once in BASE_SHEET (all 17 hosts adopt) + delete 3 dead copies; all 5 scopes fire, mid-animation clicks land, reduced-motion 1e-05s. **QW3 `qw/keys` `c873aef`**: tab key-badges (desktop-only, tab geometry byte-identical 44px), `aria-keyshortcuts` 0→26, keyov grade row wrong on 3 axes (said "1 2 — Solid or Revisit"; reality 1=Missed 2=Shaky 3=Solid) fixed, **bonus defect fixed: modifier chords leaked into single-key handlers** (Ctrl+P double-fire, Ctrl/Cmd+F focus hijack, Alt+← walkthrough step); cold probe 74/74 vs base 50/50; gate 40/40; **12/16 VR baselines re-captured for one justified cause** (the at-rest badge column), 4 must-not-change sha256-proven byte-identical. Pre-merge note: QW2's loaded-box runs were 38–39/40 with reds attributed to sibling load (every check green in ≥1 full run) — the serial merge gate doubles as its quiet-box 40/40 run-of-record. Not merged, not pushed.
- [2026-07-18 — Nits batch (test-infra / overlay-css / overlay-logic): 3/3 CLEAN, merge-ready](2026-07-18-nits-batch.md) — three deferred-nit lanes off base `54596e7`, each independently cold-verified with negative controls on base; one bounce total (overlay-css round 2). **`qw3/nit-test` `52507c2`**: `waitPainted`'s effective-opacity walk now **crosses shadow boundaries** (`getRootNode().host` hop) — on the real page the old walk reported 1.0000 while `.push`'s true effective opacity sat **below the 0.95 CORE_ALPHA gate in 4/12 contexts** (a light-DOM `#drill` fade above the deep-drill host; the `9c12244` "no core glyph pixels" flake class, one load-shift from recurring); teeth proven (old false-greens a held shadow child in 21–88ms, new refuses then converges), light-DOM callers behaviour-identical by construction AND `===` proof, cta_contrast faster not slower (1498/1899ms vs 3554/2075ms inside-mechanism); plus check_all's click_drift comment now cites the **deterministic 10-assertion backbone** instead of "14-15 (timing-dependent)" (Python-AST-identical, provably comment-only). **`qw3/nit-css` `85a50ef`** (2 rounds): the seven-dialog overlay family unified to **one frame — width 560/324, top-hung topGap=18 on 28/28 opens** (y stays 18 while mock content grows +150px — growth extends down, no re-center jitter), bottom-flush 14/28→0, radius/shadow/backdrop computed-identical ×7; the bounce closed r1's **mobile tap-floor regression** (pills 24/24 @360 → 44px re-asserted inside the ≤919px block, chosen over `min-width:920` scoping to avoid the 919–920px no-floor sliver) and corrected **three prose figures to the committed JSONs**; verifier 117/0 across 14 arms; only 2 baselines re-captured, all changed pixels confined to the 931×21 pill-row box at zero tolerance. **`qw3/nit-overlay` `e3096d1`**: keyov now **touch-reachable** (removed `kbd-only` from #keyopen + deleted the zero-consumer mobile hide; grid clip fixed by a **2-col ≤419px query, deliberately NOT auto-fit** — desktop control stays 3 tracks, verifier's 420px boundary probe green) + `.mock-body:focus-visible` ring == resolved `var(--acc)` **6/6 room×theme** (offset −2px inward for the flush rounded corners; covers #mixbody by class) + mobile Tools sheet **bottom scroll cue** (the v90 seg-scrim recipe rotated vertical, gradient seamless to `var(--card)` both themes, returns on scroll-up, reduced-motion safe, desktop `content:none`). ⚠ One flag: nit-overlay's fixer-archived gate capture is RED from a Windows spawn fault (`0xC0000142`) — the **verifier's clean full gate on the same commit is 40/40** and the gate rebuild byte-reproduced the committed deliverable; take the serial merge gate as run-of-record. Not merged, not pushed.
- [2026-07-19 — Post-ship cold audit (8 blind lenses, Direction A complete)](2026-07-19-postship-cold-audit.md) — first fresh-eyes audit of the live app since Direction A shipped; lenses BLIND to the known-threads list, synthesis reconciles. **26 items: 0 P0 / 3 P1 / 10 P2 / 13 P3.** P1s: focus-mode escape-trap (hides its only exit, Esc dead, mobile dead-ends); first-visit drill pane switch 218–326ms breaches 250ms (heaviest pane, despite eagerTopic — activation still pays a build); dark-mode Continue dock collapses to the primary Mock CTA's visual tier (hierarchy only works in light). **Certified positives:** content parity REAL in lived quality (saga == flagship pane-for-pane, bundle-verified), flow-loop receipts truthful end-to-end (dock==pip==#ssgo==flowRec at every checkpoint; restore honest, no regrade), judge→advance ~44ms steady @4x, armed legend = best design decision (legend-not-CTA held, AA everywhere). Fresh W2 defect root-caused: drill debrief misclassified as judgment point (_judgeOn never cleared → dock arms phantom keys, `n` inert at the first terminal). Six direction candidates D1–D6 (no-dead-ends / grade-at-reveal+cold-open / first-visit perf / W2 polish / mobile parity / trend integrity). Provenance: first pass ran 4/8 real lenses (3 stubs + 1 schema-death, honestly recorded); 4 re-run hardened as standalone agents. Raw lens returns: workflow wf_f0107124 journal + agent transcripts.
