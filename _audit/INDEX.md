# deepdive-rehearsal — audit index

- [2026-07-11 — COMPILER PARITY FIX, independently verified](2026-07-11-compiler-parity-fix.md) — a skeptic's re-verification of the claim that the compiler was made lossless. **The thesis is confirmed: the depth gap was an ENGINEERING failure, and the fix is real.** Root cause, definitively: `TOPIC_MARKDOWN_FORMAT.md:238` calls System stages a bullet list while its **own worked example at `:251-253` writes them as plain lines** — all 38 authors copied the example, markdown-it merged the adjacent lines into one paragraph, and the first matching field swallowed the rest. **3 of the spec's 12 worked examples did not survive the parser that documents them.** Re-derived from scratch with instruments importing none of the fixer's tooling: **+608 items recovered by engineering alone** (189 sys stages, 114 tier notes, 76 pivot answers, 76 beat models, 76 interviewer questions, 38 stage-cursors, 38 curveball themes), parity **26% → 36%** on the prior audit's metric (**17.9% → 23.2%** counting every slice the 8 have). Browser-verified on all 46: **System Maps 0/38 empty (was 38/38), 189 stages painted; literal `undefined` gone (0 of 3 sites); 46/46 cross-topic jumps land correctly; 0 console errors; THE 8 byte-identical and intact.** **The gate is no longer self-confirming — proved by restoring the old parser and re-running it: the new gate FAILS (conservation, doc-examples, compiler_md 7-fail, build_integrity) on the exact parser the old gate passed 19/19.** The remaining depth gap is **genuinely authoring** — `Follow:`/`Senior:`/`Speak:` appear **0 times in all 38 `.md` files** vs 173 each in the 8, and a format-ceiling probe proves the markdown *can* express them (`FORMAT CEILING: none`). **⚠ ONE LIVE DEFECT REMAINS, and it is engineering, not authoring: the coaching rail still shows ANOTHER TOPIC'S note on 64% of pane-topic combos** (266/414) — `shell.js:237` `if (TOPIC_CMP_NOTES[tab]) {…}` has **no `else`**, so a topic with no note for the active pane keeps the previous topic's note on screen; caught cold in the shipped build (on `caching`, the System Map rail serves **IaC's** note). Not fixed, not caught by the gate. Evidence: `_audit/2026-07-11-compiler-parity/` (5 verifier-authored instruments + proof shots).
- [2026-07-11 — FULL STATE AUDIT (16 lenses, adversarially verified)](2026-07-11-full-state-audit.md) — the system of record for the whole app. 16 lenses (5 inventory / 8 runtime-Playwright / 3 visual-design), each re-verified by a second agent that tried to refute it; refuted claims stripped. **Verdict: a fundamentally sound app with a broken build-time content path and four one-line show-stoppers — nothing needs a rewrite, almost nothing needs cutting.** Headlines: (1) **the app renders a BLANK PAGE under `prefers-reduced-motion`** — `styles.css:90` sets `body{opacity:0}` and `:137`'s `animation:none!important` deletes the reveal; 4 lenses caught it, the a11y lens *certified it passing*; 1-line fix. (2) **On a phone the Tools button — the ONLY entry to 12 tools — is off-screen on 16–24/46 topics and unreachable at ANY point on the display on the longest topic even at 430px**; one missing `min-width:0` (`styles.css:825`); 1-line fix. (3) **46 topics = 38 markdown + 8 legacy JS**, and the 38 are **28% of reference depth** — because the compiler **silently discards 189 authored System-Map stages, 114 tier notes and 76 pivot answers on every build** (`parse_md.mjs` needs bullet lists; authors used the plain-line form *its own docs show*) → empty System Maps, `undefined` on screen at 3 sites, 2,100px chips clipped mid-word, dead cross-topic jumps, mis-navigating jump buttons. A verified shape-only reshape recovers all ~380 items with **zero compiler changes**. (4) The **Cram sheet + Scope overlay serve Content Pipeline's content on 45/46 topics** under a correct header. (5) The app's own **"Drill my 3 Revisit probes →" button destroys a completed 22/22 run** (`{done:22,tot:22}` → `{done:1,tot:3,revisit:[]}`). (6) The **visual trainer draws nothing (0×0 canvas)** but is **2 × S-effort bugs from working** — sim 11/11, pipeline correct, pilot renders; its only working configuration is reduced-motion, i.e. the blank-page one. (7) **The 5.1MB is NOT a problem** — stripping 25% of the bytes made boot *slower*; boot is layout-bound. **Root cause of everything: the gate tests truthiness, not population (`if (!data[v])` — `{stages:[]}` is truthy); every defect shipped through a green 19/19.** 21 findings P0→P3, full 46-topic inventory table, prioritized visual plan, decisive KEEP/DROP (**DROP all 20 non-master branches + 3 of 4 archive tags; DROP zero topics; zero orphaned modules — the rot is in DATA, not code**), 4 strategic options + recommendation (Wave 0 one-liners → harden the gate → compiler fix → content & visual tracks in parallel). Evidence: `_audit/2026-07-11-state-audit/` (16 lens + 16 verify reports, ~120 scripts, ~400 shots).
- [2026-06-29 — Keystone A: Topic Conductor Build-Ready Design](2026-06-29-keystone-a-design.md) — the full build plan for the per-topic DATA contract (the gap-analysis's Keystone A). **Mechanism**: `TopicPane` base class + `TopicRegistry` singleton + one `deeptopicchange` event; `setTopic(id)` reseeds cross-pane globals + rewrites scattered identity, fires ONE event, every pane re-invokes `renderTopic(slice)` IN PLACE (host identity / shadowRoot / adopted sheets / listeners all preserved — uniformity of a single switch without recreating elements; mixin rejected). Covers: best-of-both protocol + why; canonical base API (topic-protocol.js, full code) + init/renderTopic lifetime split; full per-topic schema (+ wb.sub/okVerdict, sys.heads, num.compute extensions); disjoint-file layout (per-pane ownership table); setTopic ordered sequence + router topic-axis + identity-binder; per-pane table + all 9 specs; foundation-first build order (F1–F6 → 9 parallel worktree panes → topic-2 proof) + hard-pane analysis (drill L / wb M by LOC; trade/rf/open/num by novelty). **8 honestly-flagged gaps** incl. abbreviated schema, non-uniform foundation aliasing (model forward-ref + 3 fully-baked no-alias), the shared unit_tests.py manifest merge-conflict risk, and topic-2 = real authoring.
- [2026-06-29 — e2e / Playwright Verification Layer](2026-06-29-e2e-verification-layer.md) — closes the #1 documented risk: THE GATE now RUNS browser checks instead of skipping everywhere. Portable Chromium detection (`playwright.chromium.executablePath()`, no hardcoded /opt + NODE_PATH), package.json devDep, CI installs playwright+chromium before the gate, new `test/e2e_interactions.cjs` (theme/text-zoom/must-hit/rescues, 0 console errors), + a render.cjs flakiness fix the running-vs-skipping exposed (fixed 70ms wait -> deterministic waitForFunction). master `2bef4a5`->`ebd7203`, test-infra only (deliverable byte-unchanged). **CI run 28371382740 success 53s — browser checks ran green in CI for the first time.** Honest soft spots: scroll-to-top soft assert + page-visibility wiring-only (headless can't force document.hidden; full path hand-verified).
- [2026-06-29 — Batch 1 Build Verification (rescues + must-hit-points)](2026-06-29-batch1-verification.md) — the operator's "Quick rescues + must-hit-points" batch, built in 2 worktree-isolated agents, browser-verified (Playwright, light+dark, 0 console errors), serial-merged GATE-gated, CI-deployed live. **5 features**: must-hit-points grounded drill self-scoring (`ad0bec1` — per-probe `<b>`-term checklist from each card's own answer+senior, coverage-driven Solid/Revisit rec, debrief names the dropped phrase; CPR1/getStats untouched) + 4 offline-safe rescues (`12a5b2b` — text-zoom A-/A+, pomodoro 25/5, page-visibility anim-pause, scroll-to-top). master: `e93ed7b`->`ad0bec1`->`12a5b2b`. Includes the disjoint-source/regenerated-deliverable merge recipe for multi-worktree builds. Branch cleanup (#9) now UNBLOCKED.
- [2026-06-29 — Tiered Reconsideration of Dropped/Deferred Features](2026-06-29-reconsideration.md) — all 77 previously-dropped/deferred items re-scored under the operator's "implementation ≠ value" lens (offline-unsafety is a fixable impl detail, not a reason to drop a useful feature). **12 RESCUE_WORTHY, 47 clean drops, 18 operator-taste.** Key structural finding: the 12 rescues are *exactly* the 12 Tier-2 (fixable-env) items — the lens maps precisely onto Tier-2; nothing useful is trapped in Tier-1/0/3. Strategic miss = the real-browser **Playwright e2e/verification layer** (#1 documented risk; effort L) + 4 S-effort UX/a11y singles (font-size A-/A+, scroll-to-top, pomodoro, page-visibility) + a 4-line MOBILE-SPACING test guard. Standout false-positive: `pomodoro-timer` dropped on a phantom `sessionStorage` tag (its code uses no storage). Bonus master-hygiene 1-liner: dead `animation:activePulse` in `model-answers/logic.js:15`. Supersedes the earlier 3-chunk/58-item draft.
- [2026-06-29 — Forward Gap-Analysis Roadmap](2026-06-29-gap-analysis.md) — what's genuinely MISSING (not branch salvage), synthesizing the 5 per-dimension ideation passes. Two-shaped gap: **BREADTH** (ships "Topic 1 of 8" but only 1 exists; the other 7 are named + bridge-authored in `SYS_PIVOTS` but unbuilt — content-XL) and **DEPTH-OF-MEMORY** (CPR1 is a lossy aggregate scoreboard, not a per-signal save-file). **Synthesis verdict: 5 dimensions collapse onto 2 keystones + 1 honesty substrate** — Keystone A = the per-topic DATA contract (data-ize the 4 HTML-baked surfaces + registry/router/switcher, L) unlocks all curriculum; Keystone B = the CPR2 per-signal mastery code (L) unlocks the retention half of learning-science AND the heatmap/save-game half of persistence (same upgrade, two dimensions); the must-hit-points checklist (M) grounds every self-score so CPR2 stores honest data not vibes. Ranked top-7, 5 dimension sections, 3-phase build order + orthogonal a11y track, full 36-feature table, honest XL caveats.
- [2026-06-29 — Branch Merge-Triage](2026-06-29-branch-audit.md) — all 6 branches triaged in isolated worktrees + adversarial verify (13 agents). Verdicts: `enhance-web-components` **MERGE** (fixes latent unstyled-companion bug); `visual-enhancements` **SALVAGE** (visual track + the real guard tests master's RCA over-claims); the two giants v300/v308 = **one bloated lineage** (~88% generated JSON/PNG + offline-violating network code) → file-extract the ~1.2K-LOC SPA core only, never merge; `gh-pages` **REDEPLOY** (serving a stale unmerged commit); `master` keystone fix = `.gitattributes` EOL normalization. Recommended serial plan in the report (Step 0 foundation → enhance-web-components → visual-enhancements → SPA scope gate → SPA extract → gh-pages redeploy).
- [2026-07-13 — Room-accent system VERIFIED ALREADY SHIPPED](2026-07-13-room-accent-verification.md) — dispatched to *build* accent threading off a stale brief (the 07-11 audit's "`--topic-accent` is never set / 7px dot" line); the agent rendered pixels first and proved the six-rooms system **already fully built, contrast-solved and test-guarded** (`--topic-ink/solid/wash/edge → --acc*` at styles.css:177-203, `applyIdentity` `data-group` stamp, ~690 consumer sites in 23 shadow sheets, room_contrast + room_contract + pixel baselines). Records the raspberry-not-red security rationale and the **deliberately-neutral surfaces** so nobody "fixes" the restraint. 12-shot evidence matrix in `shots/2026-07-13-rooms/` (untracked). Branch `vis/v-accent` closed as a verified no-op. **Do not brief visual work from the 07-11 audit.**
- [2026-07-13 — Visual sweep punch list (8 lenses, deduped, packaged)](2026-07-13-visual-sweep.md) — **system of record** for the 8-lens visual sweep, deduped and cut into 4 collision-free work packages along ownership boundaries. **13 actionable: P0=0, P1=1, P2=7, P3=5.** Dominant theme is **pane-content typography** — reading prose renders 12–13px (below the 14px floor) and runs 100–129ch at 1536px, all *inside the nine shadow-DOM panes `styles.css` cannot reach* (fix lives in `base-styles.js`/`shared-sheets.js`/pane modules, never `styles.css`). Lone **P1** is a recoverable routing dead-end — browser Back from the landing index → **blank white page**, independently **CONFIRMED** (0 non-white px at both viewports) though the verifier corrected the mechanism (it is `replaceState`-to-prior-document, not a push/router-no-op; `router.js:174` already replaceStates the boot hash). Packages: **PKG-A** app-shell JS routing (`router.js`, dispatch first) · **PKG-B** per-pane shadow CSS typography+measure · **PKG-C** app-shell JS group-colour single-sourcing (`groups.js` `TOPIC_GROUPS[].color` is dual-sourced vs CSS `--room`; same-group two-hue, dark 3.37:1) + index-overlay Cram pills · **PKG-D** global `styles.css` mobile tab-strip affordance (3/9 tabs @360) + 500ms motion budget. **`--topic-accent` confirmed a dead zero-consumer token — do NOT thread it; the real seam is `TOPIC_GROUPS[].color`→`var(--room-*)`.** ⚠️ Two lenses (`mobile-tools`, `overlays-desktop`) returned placeholder stubs → **desktop dialog overlays and mobile Tools are UNCOVERED, re-run before closing.** Evidence: `_worktrees/deepdive-rehearsal/v-sweep/_sweep-shots/` (536 shots).
- [2026-07-13 — THE VISUAL WAVE: 7/7 merged, gate 40/40](2026-07-13-visual-wave-shipped.md) — the punch-list fix wave's system of record: seven independently-verified merges (build determinism + guard; click-surface 55.6px→0 + guard; cta_contrast paint-settle repair; colour single-sourcing with the second palette DELETED; the shadow-pane typography overhaul; Back-navigation fix + guard; mobile tab-strip affordance + the load-bearing pin commented AND asserted). Every instrument negative-control-proven; one bounce-for-fix kept the wave asterisk-free; mobile drift certified negative by dual instruments. Full evidence map + deferred-nits list inside. **NOT PUSHED — operator decisions pending: push + homepage.**
- [2026-07-18 — "Manually constrained, no smoothness": the 5-lens root-cause synthesis](2026-07-18-manual-smoothness-analysis.md) — the operator's felt verdict, run to ground across 5 driven lenses (click-ledger, motion-map, jank-trace, input-grammar, flow-critique), load-bearing findings adversarially re-verified. **Verdict: the feeling is real and literal, but it is NOT missing capability — it is a wiring/surfacing/application problem; every lens independently found the smooth/guided version already ships and is simply not applied to the default path.** Four ranked root causes: **(RC-A, dominant, architecture)** the app is a control panel not a path — **62 of 63** journey actions are hand-driven (the whole product auto-advances in **exactly 1** place), **0 of 4** completion surfaces hand forward, **0** "continue/next" controls doc-wide, the real `pickRec()` next-move engine buried **314px below the fold** behind a modal, ~**64** controls/screen with ~**1** recommendation; **(RC-B, dominant for "no smoothness", deliberate-trade + application gap)** **~50%** of state-change types (8/16) are motion-dead hard snaps on the **highest-traffic** surfaces with `transition-duration:0s` on `#home`/pane-host/all 7 overlays and a **~111ms** un-animated swap per click — the `pop` reveal animation silently no-ops (0 `@starting-style`), HOME→index is byte-identical at +40ms vs +540ms; **(RC-C, input-modality gap, amplifier)** **22/22** keyboard shortcuts WORK but **0** hints on the 9 tabs + **0** `aria-keyshortcuts` → ~100% pointer-driven on **21/49** sub-44px targets, mobile swipe dead-ends at the 2 diagram panes; **(RC-D, performance, latent)** fast machine barely hitches (**668ms** blocked/30-interactions, 24/30 zero long tasks) but mid-range 4×-CPU explodes **21×** to **14,008ms** blocked / 8.4s boot / 2.5s frozen input. **Candid on the wave:** it correctly stripped transforms off hit surfaces (click-surface 55.6px→0) and removed `startViewTransition` (ate 150–500ms on the 11.6MB doc) — but hit-test-safe opacity/crossfade motion still ships on the overlays and was simply never re-wired onto the default path, so **most of RC-B is re-wiring debt, not a tradeoff that must stand**; the SVG bake made perf net-positive. Fix map = opacity-only quick wins (crossfade the default path, fix the `pop` no-op, surface the keys, persist sub-position, default resume ON) + structural spine (a "Continue" affordance extending the drill's proven chain, completion-as-hand-off via `pickRec`, weakness-ranked sequencing) — each tagged with root cause and the three invariants it must not break (click-surface stability, reduced-motion, offline single-file). Adversarial re-verify walked back RC-D's tail (circuit-breaker **~1.2–1.4s** not 2.4s; wb is among the LIGHTEST not heaviest) and confirmed RC-A/B/C exactly. Sharpest tell: the opener's `[drill]/[walk]/[trade]` chips imply cross-pane passage but are **decorative (0 interactive)** — the connective tissue is painted on, not wired. Evidence: `shots/2026-07-18-manual-analysis/` (68 shots).
- [2026-07-18 — Flow design judge-panel: synthesis & operator decision](2026-07-18-flow-design-panel.md) — 5 independent flow designs (continuity / session-arc / minimal-touch / homepage-led / data-led) scored by 3 citation-verifying judges. **Scoreboard: D3 minimal-touch 131 > D1 Continue-Spine 129 > D4 front-door 124 > D5 evidence-ladder 122.5 > D2 practice-session 107.5 — and all three judges independently prescribed the same composite: D3's hand-offs first, D1's spine as the destination.** Three synthesized directions — **A (RECOMMENDED): staged "close the loop, then run the spine"** (W0 un-lie `mock.last` + pickRec 6.5 + microtask freshness law → W1 = five terminal hand-offs w/ receipts ≈5d → W2 = NextUp dock + one-compute contract + `pos.<id>` cursor resume ≈5-6d; committed as a PAIR); **B: the spine outright** (same end-state, one ≈9-10d train, unseverable mobile bet); **C: front-door-led** (home TODAY plan + stagehead chip, no persistent chrome, lobby-first forever). Binding adversarial facts: there are FIVE+ terminal surfaces (TWO mock ends — `#vrestart` drill/logic.js:802-806 AND `#mbagain` mixed-fire.js:218-257; no design covered both); `mock.last` is a global topic-less lie (re-key before promoting mock recs); `_updCount` fires before `_emitGraded` → all completion recs compute on queueMicrotask; pickRec never hands anyone to a FIRST mixed fire (branch 6.5 fixes). **The homepage question is CLOSED: standalone `#home` already shipped, overlay stays the `\` switcher — the only variable is the home's job.** 12 anti-goals (no auto-cross, no second decision engine, no unreceipted guidance, no gamification, SELF-dedupe, gate-or-don't-ship). Raw designs + judge texts: workflow journal.
- [2026-07-18 — Mid-range (4x CPU) perf track: attribution → chunking prototype → cold verdict](2026-07-18-midrange-perf.md) — RC-D run to ground. **Attribution inverted the blame:** all 10 panes DO re-render synchronously per topic entry, but that script is ~6–9% of the entry task — the money is 3–4 forced full-document style+layout passes per entry (`upd()` `void offsetWidth` ×2, `_maybeRender` `offsetParent`, `pinTop`/`scrollTop`; ~450–650ms waste @4x), hidden panes' layout bills deferred to first reveal (105–521ms/switch), and the 11.6MB boot parse floor (~6.2s @4x); plus a measurement trap (the `longtask` API silently drops the big throttled tasks — trust trace `RunTask`). **Prototype** (`TopicPaneQueue`, candidate #2: active-pane-first + one-pane-per-macrotask drain + flush-before-reveal) cold-verified **PARTIAL**: real **−45% input→paint @4x with a heavy pane active, −20..−53% heavy-topic entries**, 1x strictly safe, both gates PASS — but the "855–1185ms entry task" was load-inflated (calm base 96–422ms; true saving ~100–130ms, exactly attribution's own band), work is redistributed not reduced, and **one reproduced regression: `_tpOnScreen` uses the hash as visibility oracle, so after the session panel's "Back to the drill" (default rec) every topic change paints byte-stale content on the VISIBLE pane** (390ms @4x) — violates the prototype's own invariant. **Recommendation: ITERATE** (fix the oracle from `.pane.on`, re-verify, restate the win honestly) — NOT merged; prototype lives uncommitted in worktree `perf-chunk`. Forced-reflow batching (predicted −450..−700ms/entry) is the bigger, untouched next track.
- [2026-07-18 — Smoothness quick-wins wave (QW1–3): 3/3 CLEAN, merge-ready](2026-07-18-smoothness-quickwins.md) — the three opacity-only quick wins from the manual-smoothness fix map, built off base `54596e7`, each cold-verified in **1 round** with negative controls on base; **nothing bounced**. **QW1 `qw/crossfade` `93dfae6`**: the 3 genuinely dead swaps (topic-body, `#home` enter, dropdown open) now fade 150ms opacity-only — root cause: `setTopic` repaints shadow panes IN PLACE (`.on` never re-toggles so `panein` never restarts) + two bare display flips; pane switch byte-identical, no transforms, no `startViewTransition`; gate 40/40, VR 16/16 worst 0 px, zero re-captures. **QW2 `qw/pop` `c52df50` — marquee root cause: `@keyframes` are TREE-SCOPED in shadow DOM.** 11 rules across 5 shadow scopes referenced `pop`; only the drill's scope defined it — 4/5 scopes computed `animation-name:pop` yet instantiated NOTHING (0 events, `getAnimations()`=[]); the document-level copy can't reach shadow. **Corrects RC-B's `@starting-style` hypothesis (pop is an animation, not a transition — measured and disproven).** Fix = define once in BASE_SHEET (all 17 hosts adopt) + delete 3 dead copies; all 5 scopes fire, mid-animation clicks land, reduced-motion 1e-05s. **QW3 `qw/keys` `c873aef`**: tab key-badges (desktop-only, tab geometry byte-identical 44px), `aria-keyshortcuts` 0→26, keyov grade row wrong on 3 axes (said "1 2 — Solid or Revisit"; reality 1=Missed 2=Shaky 3=Solid) fixed, **bonus defect fixed: modifier chords leaked into single-key handlers** (Ctrl+P double-fire, Ctrl/Cmd+F focus hijack, Alt+← walkthrough step); cold probe 74/74 vs base 50/50; gate 40/40; **12/16 VR baselines re-captured for one justified cause** (the at-rest badge column), 4 must-not-change sha256-proven byte-identical. Pre-merge note: QW2's loaded-box runs were 38–39/40 with reds attributed to sibling load (every check green in ≥1 full run) — the serial merge gate doubles as its quiet-box 40/40 run-of-record. Not merged, not pushed.
- [2026-07-18 — Nits batch (test-infra / overlay-css / overlay-logic): 3/3 CLEAN, merge-ready](2026-07-18-nits-batch.md) — three deferred-nit lanes off base `54596e7`, each independently cold-verified with negative controls on base; one bounce total (overlay-css round 2). **`qw3/nit-test` `52507c2`**: `waitPainted`'s effective-opacity walk now **crosses shadow boundaries** (`getRootNode().host` hop) — on the real page the old walk reported 1.0000 while `.push`'s true effective opacity sat **below the 0.95 CORE_ALPHA gate in 4/12 contexts** (a light-DOM `#drill` fade above the deep-drill host; the `9c12244` "no core glyph pixels" flake class, one load-shift from recurring); teeth proven (old false-greens a held shadow child in 21–88ms, new refuses then converges), light-DOM callers behaviour-identical by construction AND `===` proof, cta_contrast faster not slower (1498/1899ms vs 3554/2075ms inside-mechanism); plus check_all's click_drift comment now cites the **deterministic 10-assertion backbone** instead of "14-15 (timing-dependent)" (Python-AST-identical, provably comment-only). **`qw3/nit-css` `85a50ef`** (2 rounds): the seven-dialog overlay family unified to **one frame — width 560/324, top-hung topGap=18 on 28/28 opens** (y stays 18 while mock content grows +150px — growth extends down, no re-center jitter), bottom-flush 14/28→0, radius/shadow/backdrop computed-identical ×7; the bounce closed r1's **mobile tap-floor regression** (pills 24/24 @360 → 44px re-asserted inside the ≤919px block, chosen over `min-width:920` scoping to avoid the 919–920px no-floor sliver) and corrected **three prose figures to the committed JSONs**; verifier 117/0 across 14 arms; only 2 baselines re-captured, all changed pixels confined to the 931×21 pill-row box at zero tolerance. **`qw3/nit-overlay` `e3096d1`**: keyov now **touch-reachable** (removed `kbd-only` from #keyopen + deleted the zero-consumer mobile hide; grid clip fixed by a **2-col ≤419px query, deliberately NOT auto-fit** — desktop control stays 3 tracks, verifier's 420px boundary probe green) + `.mock-body:focus-visible` ring == resolved `var(--acc)` **6/6 room×theme** (offset −2px inward for the flush rounded corners; covers #mixbody by class) + mobile Tools sheet **bottom scroll cue** (the v90 seg-scrim recipe rotated vertical, gradient seamless to `var(--card)` both themes, returns on scroll-up, reduced-motion safe, desktop `content:none`). ⚠ One flag: nit-overlay's fixer-archived gate capture is RED from a Windows spawn fault (`0xC0000142`) — the **verifier's clean full gate on the same commit is 40/40** and the gate rebuild byte-reproduced the committed deliverable; take the serial merge gate as run-of-record. Not merged, not pushed.
- [2026-07-19 — Post-ship cold audit (8 blind lenses, Direction A complete)](2026-07-19-postship-cold-audit.md) — first fresh-eyes audit of the live app since Direction A shipped; lenses BLIND to the known-threads list, synthesis reconciles. **26 items: 0 P0 / 3 P1 / 10 P2 / 13 P3.** P1s: focus-mode escape-trap (hides its only exit, Esc dead, mobile dead-ends); first-visit drill pane switch 218–326ms breaches 250ms (heaviest pane, despite eagerTopic — activation still pays a build); dark-mode Continue dock collapses to the primary Mock CTA's visual tier (hierarchy only works in light). **Certified positives:** content parity REAL in lived quality (saga == flagship pane-for-pane, bundle-verified), flow-loop receipts truthful end-to-end (dock==pip==#ssgo==flowRec at every checkpoint; restore honest, no regrade), judge→advance ~44ms steady @4x, armed legend = best design decision (legend-not-CTA held, AA everywhere). Fresh W2 defect root-caused: drill debrief misclassified as judgment point (_judgeOn never cleared → dock arms phantom keys, `n` inert at the first terminal). Six direction candidates D1–D6 (no-dead-ends / grade-at-reveal+cold-open / first-visit perf / W2 polish / mobile parity / trend integrity). Provenance: first pass ran 4/8 real lenses (3 stubs + 1 schema-death, honestly recorded); 4 re-run hardened as standalone agents. Raw lens returns: workflow wf_f0107124 journal + agent transcripts.
- [2026-07-20 — Correctness wave (D1+D6+D4-slice): 5 audit fixes, cold-verified CLEAN](2026-07-20-correctness-wave.md) — focus-trap exit chip+Esc; FAB/CTA tap-intercept; trend topic-tagging (legacy discard-never-misattribute, un-double-encode); drill debrief→MESO (real CTA + live `n`, phantom legend gone); scoreboard "This run" relabel. Gate 45→49. Verify: [coldverify](2026-07-20-correctness-coldverify.md) — teeth re-proven incl. the flake-fix (revert-only-fix-2 → exactly 2 red). Shipped dfbfe9d.
- [2026-07-20 — D4-polish wave: dark-dock P1 + flow-spine a11y](2026-07-20-d4-polish.md) — dark dock flattened below the Mock CTA (legend contrast IMPROVED ~7.0–7.6:1); N documented; dock CTA announced via dedicated region; pip named+centered; .flow-go ring; #11 dedupe verified-and-SKIPPED. Verify: [coldverify](2026-07-20-d4-coldverify.md) — CLEAN; **#11 skip upheld on CORRECTED evidence** (sticky sidebar CAN co-show dock+strip; the identical copy is the flow_contract-mandated invariant). Shipped 077d91c.
- [2026-07-20 — D5-mobile wave: the Beat-4 arc closed](2026-07-20-d5-mobile.md) — quiet NextUp kicker-chip in the fixed bottom bar (meso-shows/micro-hides, tap navigates, one-row no-growth); chip deliberately NOT on dock tokens; merge-down folded D4's announce into mobile. Verify: [coldverify](2026-07-20-d5-coldverify.md) — CLEAN, seam proven both directions, VR byte-exact. Post-merge CI caught a FONT-DEPENDENT negative control (Ubuntu fits the label unwrapped) → deterministic plant 89a6a52. Shipped 4c195ee.
- [2026-07-20 — D2-product wave: grade at the moment of truth](2026-07-20-d2-product.md) — the self-grade row at the REVEAL (was unreachable without exhausting every follow-up chain); cold-open value prop; whole-system search prompts route to component topics. Verify: [coldverify](2026-07-20-d2-coldverify.md) — CLEAN; record-invariant by own instrument (latest-wins, cov{0,0} honest); boundary-guard teeth proven. Copy finalized under operator delegation. Shipped 794fe8a.
- [2026-07-20 — D3-perf wave: the evidenced no-op that ends the chase](2026-07-20-d3-perf.md) — all three audit perf mechanisms INVERTED (reveal-layout + six-rooms retint, not drawCard/prebuild/reseed); content-visibility measured a REAL calm win (trade −105ms) then DROPPED: containment rasterizes the whole visible pane sub-pixel differently (16.8k px vs the 32px budget) — the zero-VR invariant held. Ships the instrument (_perf/) + this record; deliverable byte-identical. Shipped 041e4af.
- [2026-07-20 — Content-catalog sweep: all 38 markdown topics read end-to-end (41 Opus-5 agents, model-gated)](2026-07-20-content-catalog-sweep.md) — the coverage gap every audit stepped around: 38/38 cold reads vs a flagship-distilled rubric, saga as calibration control (HELD — but saga scored below the pool median, so "above_standard" ≠ shippable: 16 of the 28 above-standard topics carry a P0). **305 findings: 18 P0 / 81 P1 / 145 P2 / 61 P3.** 15 of 18 P0s are the topic CONTRADICTING ITSELF (correct text one pane away; readers drafted replacements) — even the reference saga carries one (ungated UPDATE after ON CONFLICT). Three dominant classes look like ONE template that missed an upgrade pass: Bank Int answered by its own Model (~18 topics, SCALE/DESIGN cards), Walk ships the anti-pattern its own Red Flags condemns (~12), Trade-off tell = virtue list with no flipping axis (~21 — promoted verbatim into the cram sheet's "Senior tells"). Cross-file P0: debugging contradicts consistency-models on clock ordering. Five waves shaped (§7, all text-only, browser ceiling doesn't bind): A p0-floor (~1d, BLOCKING) → C-checker+D-harness (cheap, durable gate checks) → B bank-regeneration (~2-3d) → C-fixes → E propagate-the-winners.
- [2026-07-20 — Wave A p0-floor: the 18 P0 factual defects](2026-07-20-p0-floor.md) — 18/18 fixed across 16 topics, 0 skipped, 0 unreproducible; gate 52/0/0; VR zero churn (proven by baseline tree-object hash at verify). debugging↔consistency-models reconciled in favour of consistency-models (both claims verified from source by fixer AND verifier). Four fixes go beyond the reader's draft on evidence and held under execution-level attack ([coldverify](2026-07-20-p0-floor-coldverify.md) — SQL run in node:sqlite incl. the rejected ON UPDATE CASCADE alternative; lambda's dead branch proven across all 150 legal inputs, new crossover 43%; receipts re-proven with the present-at-master control, 100/100). Five extra same-defect sites fixed (mostly Senior lines that would have endorsed the old error post-fix). Durable lessons banked for Waves B–E: compiler structural limits (one caption/code block, ≤2 paragraphs/drill answer — build early), strict-ASCII corpus, and the Shiki span-split grep trap (receipts must run against a tag-stripped render; both fixer's and verifier's fresh instruments had first-attempt bugs caught only by their own controls).
- [2026-07-28 — Wave C part 1: the cram-surface checker + ratchet baseline](2026-07-28-cram-surface-baseline.md) — the first time anything read WHAT THE CRAM SHEET SAYS (`cram_scope_distinct` proves each topic renders its own sheet; `topic_contract` proves the slices are populated; neither reads a word). Ships `test/cram_surface.cjs` + a may-only-shrink baseline, gate 55/55 (post-union 56), deliverable byte-identical (`46ac5661`). **The sweep estimated ~14 topics; the measurement says 35/46 — but the shape matters more than the count:** 46 of the 48 `dangling` hits are `wb.steps[].a`, and `wb.steps` is `{c, a}` — a CUE and an answer — with `cram-derive.js:91` lifting `.a` alone. saga's *"Only the ones before the pivot"* is clear under its own cue and unreadable without it, so this is **ONE composition defect replicated 46 times, not 46 authoring defects**; the cheap fix is plausibly one composer change (not made — tool-only wave, and it needs a design call + VR). Two of the brief's four flag classes died on contact with data and are reported as zero rather than widened: non-numeric ceilings would have manufactured **101 false failures** (the corpus ships legitimate verdict rows — "pull (on-read)", "n/a", "~8,333"), and exact cross-section duplicates are zero, so `dup` is a near-match whose threshold is ANCHORED on the sweep's own worked example (kafka-internals' Opener pair, 0.657) rather than picked. Watched RED four ways: synthetic plant (exactly 5 new, one per class, at the planted paths), baseline-green + byte-identical baseline rewrite, **8 detector neuterings all aborted** by a per-run self-test (which exists because the ratchet stops protecting a class once its baseline empties — the end state Wave C part 2 is aiming at), and all three mirror-drift guards proven able to fail. Honest limit: this covers the MECHANICAL half of Class H only — 4 of the sweep's 7 named instances; `feature-flags` comes back CLEAN while carrying two, so an empty baseline will NOT mean Class H is closed. § 4 is the C-fixes work-list.
- [2026-07-28 — Wave D part 1: the numbers-lattice harness + ratchet baseline](2026-07-28-numbers-lattice-baseline.md) — every topic's Numbers `compute()` driven across its declared input lattice (~568k evals in-gate; full-density 4.46M confirmed IDENTICAL findings). 21 findings / 15 topics tiered 3 teaching / 6 visible-wrong-number / 12 swallowed-NaN; sweep's ~19-topic estimate corrected DOWN by execution (1 dead flag, 0 constant rows corpus-wide). Dual-judging (full lattice + strictly-positive) exists because v1 ran GREEN on the known lambda defect — the real pre-fix compute is now permanent fixture #10. Coverage asserted against the authored markdown count (the '8/8 driven while covering 8/46' catch). [Tools coldverify](2026-07-28-tools-coldverify.md): CLEAN both tools, 13 baseline samples zero fabricated, discards confirmed right; carry-forwards F6 (two Class-G exemplars NOT covered — ride D part 2 separately) + F3 (checker clean-list is a floor).
- [2026-07-28 — Wave C part 2: the cram fixes — ratchet 66 -> 0](2026-07-28-c-fixes.md) — **43 of the 66 retired by ONE change to one line of the composer**, not 43 content rewrites: `deriveCram` now lifts the whiteboard CUE alongside its answer (`.cs-cue` muted + the sheet's own `.cs-arr`, one 30-byte CSS rule on an existing theme token, both themes screenshotted), so *"Only the ones before the pivot."* arrives as *"Step order — which steps even need a compensation? → Only the ones before the pivot."* **The receipt that the composer route beat the per-item route: `feature-flags` was listed CLEAN in the baseline and was shipping >=5 detached answers (coldverify F3) — a per-item pass driven by the work-list would have left every one of them in the artifact; all five now stand alone on the rendered sheet.** Two traps caught while wiring the mirror: (1) feeding the COMPOSED line to `dup` would have dragged `notifications` 0.68→0.58 and `rules-engine` 0.72→0.57 under `DUP_MIN`, **silencing two real duplicates without one word of duplicated text changing** — so the mirror now measures two units on purpose (`dangling` on the line the reader meets, `dup` on the sentence); (2) reverting the composer slips guards 1-3 entirely AND leaves `dangling` green, because the mirror composes the line itself — hence a **fourth mirror guard**, watched-red at **415 drift failures**. Three cues reworded rather than loosening the detector (the baseline's explicit instruction). Then 12 `when-conj` (a **pane** defect too — `trade-offs.js` renders these after a literal "pick when" pill, so 36 rendered stutters, not 24), 6 `dup` differentiated by ROLE not paraphrase (incl. the sweep's named kafka-internals Opener pair), 2 Opener `dangling`, and **Class E: 19 Trade-off tells given the trigger that flips the call** — all 155 tells in the 22 flagged topics read against the sweep's own test; 19 fail it, the rest already carry a trigger (2 of the sweep's 6 named quotes no longer exist in the corpus), every left-alone one justified in §3. Gate 54/54, VR 16/16 zero churn. **An empty baseline is NOT "Class H is closed"** — the semantic half (feature-flags' "instant", the pre-correction cues, error-propagation's un-localized flag) is invisible to any structural detector and remains open. Mechanism kept as a regression guard; verified the self-test still aborts at the empty end state (6 fixtures, before a browser launches).
- [2026-07-28 — Wave B pre-pass: the bank-pushback instrument's derived work-list](2026-07-28-bank-prepass.md) — the DEFINITIVE Wave B scope, superseding the catalog sweep's estimate with a measurement over all 46 compiled banks (613 cards, 777 Int exchanges): **170 findings across 27/46 topics**, split a-l (wB1, 96) / m-z (wB2, 74). Records where it disagrees with the sweep in BOTH directions — `api-design` is register-only and `distributed-locks` does not fire at all, while seven unnamed topics do — and flags that `consistent-hashing` is not a card problem but a whole un-refreshed bank (33 of the 170). Also warns that saga's own SCALE/DESIGN cards are themselves Class A even though saga is the donor register for everything else.
- [2026-07-28 — Wave B part 1 (a-l): the bank Ints that push back — ratchet 170 -> 107](2026-07-28-bank-alpha.md) — ships `test/bank_pushback.cjs` (gate row **57**, AST-exact) + a may-only-shrink baseline, then regenerates **30 cards** in the a-l half. **The metric the sweep proposed does not work and was not used**: ">60% content-word overlap" scores multi-tenant DESIGN — one of the four instances the sweep names verbatim — at **10%**, because its Model says "signed claim" where its Int says "verified JWT claim". The axis that separates is NOVELTY (content words the answer adds that its Model lacks), bracketed by the corpus: the four named defects carry **5/9/10/11**, the 30 donor pairs carry **48 minimum**, `NOVEL_MAX` sits at 20 inside that gap with both sides shipped as verbatim fixtures. A fourth class the sweep names — semicolon-chaining — was measured and **DROPPED**: rate-limiting's SCALE card is an unambiguous old-generation checklist with ZERO semicolons while 43 refreshed cards carry three or more. Watched-red **six** ways (baseline-green; `--plant` = exactly 4 new, one per class; all four detectors neutered; both threshold directions; and `_generated/` absent yielding **39 coverage failures** rather than a confident "8 topics, 0 defects"). Two self-test fixtures failed on first run and **both were real** — one exposed that any answer under 21 content words fires unconditionally (now stated in the header, not hidden), the other a detector bug where scanning to the first LETTER judged idempotency's "1,000 x 86,400..." by the multiplication sign's lowercase `x`. **The honest headline: the instrument MISSED three real Class-A cards** (api-design SCALE/DESIGN, consistency-models SCALE) which restate their Models in fresh vocabulary — the documented paraphrase blind spot, observed live — so a green `bank_pushback` is NOT evidence the class is gone. Gate **57/57 PASS**, VR 16/16 zero churn (Bank renders in the mock overlay, uncaptured at rest). **`consistent-hashing` (33 findings) is a deliberate justified survivor**, not an oversight: 14 cards and all 14 open lowercase — it wants its own single-file wave.
- [2026-07-28 — Wave B omega half + the dual-charter cold verify](2026-07-28-bank-omega.md) — m–z regeneration, 74/74 cleared (43 cards / 13 topics, 0 new); the builder discarded its own overlap-axis scanner on evidence. [Bank coldverify](2026-07-28-bank-coldverify.md): CLEAN — 28 cards read as a senior interviewer (zero Ints answerable from their Models; saga's regenerated pair judged best-in-wave), paraphrase hunt found 2 PRE-existing twins (N1 distributed-locks SCALE, N2 circuit-breaker recovery — one-card fixes), Int2 collateral zero-damage, VR by baselines tree-hash, merge simulated conflict-free with the deliverable size predicted to the byte. **CARRY-FORWARDS to the consistent-hashing wave: N1, N2, N3 (api-design↔idempotency tiebreak contradiction), N6 (this line is the survivor's omega-side record: consistent-hashing = ALL 33 remaining entries, an entire never-refreshed bank), + the kept-Int2 novelty-snapshot check design (bank-alpha freeze §5b) + N4 (self-test anchor docs say floor 5/ceiling 48; measured 4/51 — doc-vs-code number drift, correct when touched).**
- [2026-07-28 — The consistent-hashing coda: the kept-Int guard + the last 33](2026-07-28-chash-coda.md) — **`bank_pushback_debt.json` is now `{}`** (33 → 16 → 0, zero new findings at any step); gate **58/58, 0 SKIP**; VR 16/16 zero churn. Ships `test/bank_novelty.cjs` + an 820-entry novelty snapshot — the check bank-alpha §5b designed, **built with a different primary axis because 5b's own anchoring does not survive measurement.** "sharding DESIGN Int2 58 → 44" is that card's SHIPPED state, not a discarded draft (re-measured 42bf6eb vs 45ec7fd, confirmed by omega's own freeze table, and the cold verifier READ that shipped card and passed it — so bank-coldverify A3's sharding row is right about the card and wrong about why). The designated must-fire case is therefore a card two readers judged good, and **three kept pairs drop harder than it does** (0.740 / 0.750 / 0.755 vs 0.759), one of them also read-and-passed. The confound is Model growth (those Models grew 5-6x; sharding's 1.9x), and single-sentence punchline plants land at ratio 0.63-0.94 — straddling the legitimate population entirely, so the ratio axis cannot separate growth from theft. **Contiguity can:** the corpus's longest legitimate shared run is 8 (691 of 692), the same plants reach 9-42 — and the 692nd exchange, at **19**, is a REAL defect this check found on its first run that wave B *and* the cold verifier's paraphrase hunt both missed (`state-machine` SCALE Int2 restating its own Model's ceiling derivation, novelty 75 so `thin_int` was structurally blind). Fixed; the fixture keeps the pre-fix text. Both thresholds bracketed from both sides and shipped as fixtures **extracted from the corpus, never retyped** — which is exactly what caused **N4** (also corrected here: the enforced bracket is [4, 51] not [11, 48], the floor fixture is a paraphrase and not verbatim as its header claimed, and a third wrong number — an inline "novelty 106" that pair never had). Watched-red **seven** arms, incl. two plants built to fire ONE arm each (proving the arms are not proxies). Then the bank: **14 cards / 20 exchanges regenerated**, novelty 3-21 → 90-130, and the echo arm clean on the wave's own 14 Model rewrites (longest run 2-4 vs a legitimate max of 8) — the highest-risk instance of the hazard, measured rather than asserted. Carry-forwards **N1** (distributed-locks, 29 → 124 — the new Int attacks the Model's own first recommendation: per-RUN idempotency stores one bit and a half-finished billing run is neither started nor finished), **N2** (circuit-breaker, 30 → 141 — declines a fourth knob and moves admission control to the dependency, since local controllers cannot see a global capacity), **N3** ruled FOR the donor on failure asymmetry, keeping api-design's real contribution and separating "include when unsure" from "hash the whole body", and **N8**. The N8 fix is the cleanest proof of the guard's purpose: a Model edit over two KEPT exchanges, measured at 117 → 114 and 82 → 80 (~97% retained). `build_integrity`'s HEAD-match arm was DEFERRED in-gate by the untracked capture file (the trap that took it from three wave-B agents), so the capture was committed and the check re-run clean: **COMMITTED deliverable == fresh build of HEAD**. Empty baseline does NOT retire the instrument — `--plant` still yields exactly 4, one per class.
- [2026-07-29 — Wave E: propagate the winners (the ladder's last rung)](2026-07-29-wave-e.md) — the campaign's only wave that adds STRENGTHS rather than fixing defects: 11 files, **54 insertions**, every one citing a named donor. **The brief's pass 3 was reshaped by inventory before a word was authored** — `no_int2` is scoped to SCALE/DESIGN and Wave B left **zero** of those missing, so the surface is the other **326** cards; the funnel ran 326 → 45 (Models that add a component) → **3** (strict filter), with circuit-breaker `the protection is the dependency` and multi-tenant `the cache` rejected because they *already are* cards about the added component's own failure. **Class I's unnamed fourth recipient derived on evidence, not guessed**: ranking timeout density against `idempoten*` coverage found **backpressure** (29 timeouts, **0** idempotency, prescribing deadline-drop and shedding throughout) — and the same measurement *cleared* kafka-internals (87), distributed-locks (73) and leader-election (27), which look like candidates on raw timeout counts but resolve the ambiguity by mechanism rather than vocabulary; developer-platform (45 timeouts, 0 idempotency) examined and **skipped with reason**. **Pass 2 is two topics because the corpus already defends itself** — slos (0.999³ = 99.70%), probabilistic-structures (1.44 = 1/ln 2; 1.04/128 = 0.81%) and caching (the 90→95 hit-ratio halving) all re-derived and found correct — leaving load-balancing (`log log n` is the **excess over the mean**, not the load: ~10,002 vs the "about 2" people say, wrong by ~5,000x; one-choice excess `sqrt(2·(m/n)·ln n)` ≈ 303) and devices-dispatch (C10K solved the event loop, not the **buffers** — TLS at ~32 KB/connection makes 50,000 sockets ~1.6 GB, not 200 MB). Optional judged item **RESOLVED not deferred**: replication's semi-sync recommendation now names k-of-n at the **only two panes lacking an adjacent correction** (Walk + Whiteboard), the self-correcting SDE2 and Staff cards deliberately left alone. Three conventions measured before authoring and honoured: Follows run 2/card (5 at three; this wave spends 3 more, 8/953 = 0.84%), **all 1,603 Follow answers are one paragraph** (no exceptions created), and every pane-level VR capture targets event-driven — so the two VR-sensitive recipients were edited **in the Bank only**. Gate **58/58, 0 SKIP**, `build_integrity`'s HEAD-match arm FIRING (capture written outside the repo, tree clean at gate time — the trap that deferred it for three wave-B agents); VR 16/16 zero churn, worst 4 px of 32. Ratchets: pushback `{}`/613 cards unchanged, novelty 820 → **826** with longest shared run still **8 < 9** (snapshot refreshed in the same commit as each content change), cram `{}`, lattice untouched. **The three dispatched inventory agents never returned — every list is the builder's own reproducible measurement.** **BOUNCED ONCE, AND THE BOUNCE IS THE BEST RECEIPT IN THE FILE (§9):** the verifier passed the content (all three pass-3 Int2s true and seam-aimed, funnel reproduced row-for-row, every recorded skip held) but caught the builder committing *this wave's own defect class inside the card teaching against it* — the P2C Follow closed on "3% at **any** volume", conflating "the two-choice excess is a constant" (true, and stated correctly one sentence earlier) with "the percentage gap is a constant" (false: excess grows as `sqrt(m/n)` while the mean grows as `m/n`, so the percentage SHRINKS — ~3% at 10k requests/backend, ~0.3% at 1M). Replaced with the stronger absolute claim plus the scaling rule (**every 100x of volume divides the percentage by ten and multiplies the absolute excess by ten**); everything else in the paragraph survived both re-derivations. **A second, smaller self-correction sits on top of the first:** the "formula disagreement" the builder reported in the initial re-freeze **did not exist** — diagnosed from the bounce summary rather than the cold-verify itself, it misread that table's `m` column as `m/n` and its percentage as taken over the mean rather than over the one-choice peak. Recomputing reproduces the verifier's table row for row (130.3 / 10,303.5 / 1,003,034.9; wins 21.6% / 2.92% / 0.30%) — **identical formula, agreement on every figure**. The card was never affected, but the freeze report carried a false claim about a teammate's arithmetic until §9 was corrected. One real refinement survived: the scaling rule now says the absolute excess ×10 is exact while the percentage cut is only *roughly* tenfold, since the denominator grows too. Nits folded: devices-dispatch 200MB→1.6GB is **8x**, not "more than an order of magnitude"; replication's Whiteboard `R+W>N` step regains the **completed** qualifier (verified: 39 sites in-file, and its own Red Flags calls the unqualified phrasing *"the most damaging"* flag in the topic — a vocabulary error, and it was reaching the cram spine). That last fix is **declared scope-widening** — the step is pre-existing and wave E never touched it — and is independently revertible. Re-freeze: gate **58/58** again, VR 16/16, ratchets unchanged with **no snapshot movement** (all three fixes land outside the Bank). **Ten new exchanges have still been read only by the verifier's warm re-check, never cold.**
- [2026-07-29 -- Residuals: closing every recorded thread](2026-07-29-residuals.md) -- the wave that was meant to be bookkeeping and was not. **The campaign's one standing caveat was true:** Wave E's ten exchanges, read WARM twice and never cold, carry **four blocking defects** -- and the arithmetic was never the problem, it survived independent re-derivation entirely (32 KB, 1.6 GB, ~8x, 2.20, ~10,002, ~5,000x, 303.5, 3%/0.3%). The defects live in the *frame*, which is exactly what a warm reader cannot see: **devices-dispatch cited nginx's `ssl_buffer_size` as proof of a 32 KB/connection cost when it is the SEND buffer only, exists for time-to-first-byte, and nginx sets `SSL_MODE_RELEASE_BUFFERS` which releases those buffers on IDLE connections -- the card's own scenario**; the same card contradicted `real-time-delivery` (~10 KB/idle connection, tens-of-KB called the *careless* case) on one physical quantity, reconciled as untuned-vs-tuned; **debugging classified by status class in the card that exists to teach semantic classification** ("a 4xx genuinely did not happen") while its own tight-retry-loop storm manufactures nginx **499s** by the thousand, where the handler ran to completion after the caller hung up; and **event-driven called an unattended Kafka DLQ "fully-preserved" at six months against a seven-day default retention** -- fixed into the stronger claim, since nobody set the retention either and "lost" becomes literal. Six nits folded incl. two load-bearing ones (debugging's idempotency escape-hatch post-dates the 30,000 it claims to cover; event-driven justified idempotency with an ORDERING argument, a split this corpus names twice) and `replication`'s `R + W > N` cram-spine step, which asserted a partial write is *outside the overlap* while the file says four times it is **not rolled back and can surface later**. **Two of the findings the wave was sent to fix were themselves wrong:** cdc's Class-J entry is a FALSE POSITIVE -- *"without ever losing a change"* sits in `### Frames`, an **interviewer's design brief**, and Class J's own rationale is that the candidate *volunteered* the claim -- so cdc is unedited and a dated, scope-limited erratum was appended to the sweep instead; and F6's 13-row list needs triage before fixing, because `distributed-locks`' negative headroom is a **deliberately flagged danger state** (`over: headroom <= 0`) and `caching`'s `-2% faster` is a correct number with a wrong unit label. Item 2 found a second defect while localizing error-propagation's last Red Flag: its fix paragraph routed *permanent* failures to a dead-letter queue, **inverting the topic's own rule** (permanent fails fast; dead-letter is for an exhausted transient). `ascii_guard` widened to 807 files across four scopes with the NUL class added, **measured first**: the 38-file corpus came back clean exactly as three waves claimed, and all 59 violations were in never-scanned tooling -- including **a live raw NUL in `tools/compiler/mermaid.mjs`'s SVG cache key**, escaped rather than substituted because any other separator cache-misses all 46 diagrams, and proved behaviour-preserving by a **byte-identical rebuild**. A zero-file scope entry is now a FAIL. `check_all.py` keeps a failing check's full output (`test/_last_fail_<name>.txt`) instead of its last line -- and it paid for itself inside the wave, turning a bare `Node.js v25.2.1` into `MODULE_NOT_FOUND` instantly. Item 7 DEFERRED on measurement with a working detector handed over. Gate **58 rows, no check added**; ratchets all green, `bank_novelty` snapshot refreshed in the same commit as the 4 Bank exchanges that moved.
- [2026-07-29 — Frontend audit: 6 lenses, the next campaign's founding document](2026-07-29-frontend-audit.md) — 32 distinct findings (2 P1 / 17 P2 / 13 P3) on a frontend the audit itself calls genuinely well made. **P1s: the phone drill opens with ZERO pixels of question visible (682px scroll, re-paid every pane switch — root cause: zero short-viewport media queries in 1827 lines); the entire content area exposes ONE heading (46x9 panes at headingCount 0).** Clusters: spot fixes never generalised (#20's focus ring landed on .flow-go only — the core-loop grade buttons still wear the UA hairline; aria-current and .tn-trigger same shape); the home assembled-not-drawn (zero display-face elements, 70.3% unintended Arial, retired brand indigo); no mobile vertical budget; and the content campaign's presentation debt (flagship-tuned styles carrying 2-6.5x prose — the cram sheet is now 9.7 screens). Highest value-per-effort: flowGo bypasses the router — one line at one funnel repairs URL/title/history/Back across six affordances. Four waves shaped W1 spot-fixes (S, zero-VR-diff-is-the-check) → W2 phone vertical budget (M, the P1, iteration-shaped) → W3 home identity (S-M, operator sees before/after) → W4 long content + sidebar (M, operator gate on the typographic call). Also: audit-#8's perf numbers don't reproduce — correct before any perf wave reads them as a regression.
- [2026-07-29 -- W3: home looks like the product (display face, the Arial, the hero's room)](2026-07-29-w3-home-identity.md) -- the first-impression wave. **147 of 210 text-bearing elements on the home screen rendered in UA Arial (70.0%) -> 0**, because a <button> that never re-declares font-family does not inherit the app stack and its child spans inherit THAT. **Display-face elements on #home 0 -> 3.** The hero was painted in the RETIRED brand indigo #534AB7, a seventh colour belonging to no room; it now wears the room of the topic it opens. **The brief's prescribed mechanism was overruled by disk:** re-scoping .ix-panel's neutral would have given the hero the BOOT CONSTANT (index.html:2 hard-codes data-group and applyIdentity does not run at boot -- measured #963D86 architecture-apis while the CTA points at a messaging-events topic), so the room is stamped as --rm, the app's own per-element binding, and the neutralisation rule is untouched. **--font-size-h3 had ZERO definitions** -- the largest type on the home screen was its own hardcoded 20px fallback. Two new guards, both watched RED first: `latent_arial.cjs` (plants a bare button, reads what the UA gives it -- platform-deterministic; 19 components red, 4 fixed, 15 ratcheted to W4) and `phantom_tokens.py` (5 red, 2 fixed, 3 ratcheted). VR: **home pair rebaselined by design (210k/211k px), the other 14 at ZERO** -- and two noise-only rewrites from `vr:update` were restored rather than baked in. Before/after PNGs: `_audit/w3-home-before-after/`. **ROUND 2 (cold verify CLEAN, two non-blocking findings, both fixed):** the hero carries `data-autofocus` and is FOCUSED ON PAINT, so `button:focus-visible` (0,1,1) beat `.hm-cta` (0,1,0) for `box-shadow` and the halo was still the retired indigo -- the room-coloured shadow round 1 declared only painted while BLURRED, a state no screenshot ever sees. Two receipt rows were false as written and are corrected; the halo now derives from `--rm` via a (0,2,0) rule (scanline y=233 x=147-149: `219,216,233` -> `203,223,218` light, `42,38,60` -> `16,46,47` dark) and the global rule at `:53` is untouched. `--rm` also got a `var(--rm,var(--acc))` fallback -- without it, removing the inline attribute left a TRANSPARENT hero with a white border. Verdict in-tree: `_audit/2026-07-29-w3-home-coldverify.md` (w3-verifier).
- [2026-07-29 -- W4: long content, and the sidebar that hides the app](2026-07-29-w4-longform.md) -- the campaign's final wave, and the one carrying an **operator gate**. Sub-scope A is presentation debt: `.cs-cue` computed `display:inline` at weight 400, byte-identical to its answer and separated only by a 2.41:1 colour step, so the cram sheet's own stated contract -- *"a prompt -> recall pair"* -- had **no edge to cover** and could not be self-tested on any of 46 topics; it is now a bold block and the inline arrow is gone (that arrow still renders in `.cs-dec`, which IS an inline relation). **The `.a` slice the audit offered was MEASURED AND DECLINED**: first-sentence slicing removes 64% of authored spine text and **guts 122 of 366 changed answers to under a quarter of their length across 37 of 46 topics**, because the corpus is verdict-then-elaborate -- what survives is `"No."` (301->3ch), `"Additively."` (466->11), `"It races."` (268->9), `"Underneath each shard."` (629->22) -- and it is uneven by construction, since 49 single-sentence answers are untouched so a 353ch answer survives whole while a 907ch step collapses to 34ch. **Two of the builder's own hypotheses were tested and DISPROVED rather than reported** (the survivors are not stage directions -- 3%; and the slice creates ZERO net-new `dangling` by the repo's own detectors, since it cuts the tail not the head), so the case rests solely on the gutting number. `.dec-tell` 700 -> 500 with the **load-bearing half the finding never stated**: `.dec-tell b` is ALSO 700, so while the base was 700 the authored `<b>` spans were byte-identical in weight to their surroundings and **324 tells' worth of authored emphasis rendered as none at all**; 1577 -> 1532 total lines, flagship untouched, contrast 5.89:1 unchanged (measured, not reasoned -- 14px is under the large-text threshold at both weights). **latent_arial's debt list 15 -> `{}`** (253 buttons, 0 in the UA font): two entries needed `font-family:inherit` not `font:inherit` because the shorthand would have set a font-size the mobile icon rule must override, and **a family declared only inside a branch is not declared** -- the first pass patched `.tools-fab`/`.nd-m` inside their `>=920px`/`:not([hidden])` rules and the guard still reported both, because the walk measures the default state. Sub-scope B: **the audit's P2-4 premise is stale and is corrected here** -- the `<=600px` wrap it says to "lift" has been DEAD CODE since W2 merged (`.side-id .tn-trigger{flex-wrap:nowrap}` at `<=919px` is later and more specific; measured `nowrap` + 1px at 360/500/600/919), so the `>=920px` rule is written fresh and the dead pair deleted; `.tn-current` 18/146 -> **103/142** at every desktop width. P2-6 done as a **DOM move, not flex `order`** (`styles.css:628` records that `order` reorders paint and never tab order -- the nav is now tab stop **9 of 35, was 13**): 4 -> **7 tabs above the fold**, `.seg` top 617 -> 477. **Honest shortfall recorded rather than bought**: the 7th tab misses FULL visibility by 3px, and shaving a spacing token to close it was available and refused as the Goodhart move the guards exist to prevent. Guards: new `sidebar_geometry.cjs` (gate **66 -> 67**), watched-red at 11/13 on the pre-fix build, thresholds bracketed between two non-overlapping measured populations (ratio pre-fix 0.063-0.243 vs fixed 0.365-1.000 -> 0.30; plus a name-independent clientWidth floor, since the ratio conflates box width with title length) -- **the first draft's 0.40, taken from ONE topic, would have failed the very build it ships with**; a cram-height ceiling of 9000px anchored on the post-fix corpus max +15% (the room one extra authored step needs) whose watched-red is a `--plant` (10661px) because no natural red exists; and a 6th `focus_ring` arm for `.piv-jump`, the fourth member of P3-6. **Both negative controls in the new check had real bugs caught by running them on both builds** -- one went NEGATIVE on the pre-fix build and failed for a reason unrelated to the app. Gate **67/67, 0 FAIL, 0 SKIP** after the team-lead ruling (`995156a`) **authorized all four contested baselines** -- the brief's list was internally inconsistent (it authorized a sidebar wave while demanding byte-identity from `drill-light`/`drill-dark`, which CONTAIN the sidebar) and the `m-walk` condition too narrow; the builder's round-1 diff-reads stood as the evidence, and on the rebaseline the other **twelve baselines came back BYTE-IDENTICAL**, so there was no jitter rewrite to restore in either direction. Sub-scope A **accepted as built with both declines RATIFIED** (the `.a` slice is FINAL for this campaign; revisit only with a better mechanism such as authored summaries). **The re-gate was not green on the first try, and the diagnosis is the point:** `overlay_deadzone` -- untouched by this wave -- died with a bare `Node.js v25.2.1`, the exact signature the residuals wave's `_last_fail_<name>.txt` dump was built for, which named the line in one read. Proven not a regression BEFORE anything was changed (deliverable byte-identical at md5 `5d868341`; in-gate PASS at `9aef376` then FAIL twice; **standalone with the gate's exact argv PASS 3/3**; zero orphaned Playwright browsers, box at 10%) -- a frame-scheduling race that only surfaces under gate concurrency. Root cause read from source: the `document_start` rAF poller exited only on *splash exists AND carries `_bs-done`*, so a splash REMOVED before any frame caught that class left `sp` null forever and hung the 60s wait -- and the removed-splash case is the one the comment eight lines below **already calls "a pass by construction"**; the wait could not reach it. Fixed by widening the exit (`d341750`, **declared out-of-brief, harness-only, zero app bytes, independently revertible**); still 35 assertions. **No re-running until green** -- two in-gate failures with a diagnosis beat a third roll. **COLD VERIFY: CLEAN, 0 blocking, 9 non-blocking** ([verdict committed verbatim](2026-07-29-w4-longform-coldverify.md), authored by w4-verifier on instruments built independently of the builder's -- every load-bearing number reproduced, and the two that did not at first (p50/p90, the line totals) reconciled to a stated convention rather than an error; the verifier's own slicer found the declined slice **worse** than the freeze reported, 125 gutted across 38 topics vs 122/37, so the decline is under-argued). Round 3 fixed all nine: **two were guard-integrity code, and one is the sharper lesson of the wave** -- `latent_arial`'s CONTROL A was `A_bareDetected: UA === UA`, an element compared against a value read from that same element, and the walker SKIPPED the probe, so nothing proved the walk could flag anything; `buttonsSeen` was printed and never asserted, so a run enumerating ZERO buttons would still print PASS. Both now real and both demonstrated red by breaking them. The repo's founding rule -- no control may exist that cannot fail -- turned out to apply to the ratchet this wave had just emptied. `sidebar_geometry`'s control 1 was likewise ABSOLUTE (`off.ratio < MIN_RATIO`) and passed **vacuously on the pre-fix build inside an 11-of-13 red**; now relative (`< live * 0.5`) and it correctly refuses there. **F-2 is the carry-forward:** "latent Arial is at zero" is true of the LEDGER, not the screen -- CDP font rasterisation shows **26 buttons still painting in Arial on Windows, including all nine `.seg` tabs**, because 17 rules declare `-apple-system,sans-serif` and `-apple-system` does not resolve off Apple platforms; invisible to `latent_arial` BY CONSTRUCTION (they carry an author family), pre-existing, and wanting its own wave with its own rebaseline. Also: `.xd-again`'s fold had quietly cost 1px of type (12px vs the 13px it rendered at) -- the only value change in an otherwise additive commit, now size-neutral. Seven review pairs in `_audit/w4-before-after/` from a committed frame-asserting script; **sub-scope A contributes zero pixels to any committed baseline**, so accepting or modifying it changes no VR.
- [2026-07-30 -- W15: the brand-indigo coda (one selector, and the first screen stops claiming a room)](2026-07-30-w15-brand-coda.md) -- closes the follow-up W3 flagged. On a cold home `.hm-brand` wore **architecture-apis magenta** -- not because the app was in that room but because `index.html:2` hard-codes `data-group` for first paint and `applyIdentity` does not run at boot, so the accent was a **boot constant**. Fixed by adding `#home .hm-brand` to the `.ix-panel` neutralisation (both themes) -- **zero new literals**, because that block's own comment already names its value "the app's BRAND indigo ... and the badge colour ... IT DOES NOT MOVE". The `--indigo` token was checked and REJECTED (`#4338CA` light, not the brand colour; it is a verdict hue with zero consumers). The block's membership rule was widened honestly in its comment: "must not claim a room", not "enumerates rooms". Assertion went into **arm 3 of `room_browser.cjs`** (gate stays **67**, not 68) and hardcodes no hex -- it reads the brand indigo live off the neutralisation and checks the mark against all six `--room-*` inks; watched RED at 4 failures pre-fix. VR attribution: **642 px in a 107x9 box at (150,46)** -- the glyphs and nothing else, identical count in both themes -- with the other 14 at zero and no jitter rewrites this run.
- [2026-07-30 crossbrowser audit](2026-07-30-crossbrowser-audit.md) — WebKit 26.5 + Firefox 151 vs the chromium gate: 10 app defects (1 P1 print, 8 reproduce in chromium = gate coverage gaps, 2 webkit-attributable), 0 typeface news, 8 waves shaped
- [2026-07-30 -- W-X1: print truth (the cram sheet reaches paper whole)](2026-07-30-w16-print-truth.md) -- the cross-browser round's only **P1**, and a 90%-right stylesheet: `styles.css:509` reset four properties on `.cram-panel` and not the two that decide whether paper gets the content, so on paper -- **where there is no scrollbar** -- a height-capped `overflow:hidden` box did not paginate, it **truncated**. Print emitted **one A4 page** in FF 151 / CR 149 / WK 26.5 alike. Real `page.pdf`: flagship **1 -> 3 pages** (1828px of 2774 clipped -> **0**), consistency-models **1 -> 7** (5122px of 6068 -> **0**) -- exactly the counts the audit's isolated control B predicted, and exactly `ceil(H/1009.13)`. **The printer uses the app's MOBILE layout** (the A4 content box is 680px, inside the `<=919px` breakpoint), which is both why the operative cap was `:2139`'s bare `100vh` and how a **dead 7-button `.cram-jump` nav strip** was found printing across page 1 -- not in the audit. **X6**: the Ctrl/Cmd+P "Print Q&A" popup is a separate document the app's `:root` never reached, so every token resolved undefined and h1/h2 computed **14px/400, identical to body copy**; it now carries a `:root` harvested from the live app with the token *names* read back out of the CSS string, so the list cannot rot (24/16/13/9px, weight 800, and it follows the reader's density setting). **X9**: BASE_SHEET's print block and the `.cs-*` vocabulary were disjoint -- atomic units only, `.cs-sec` deliberately excluded (1289px against a 1009px page box is a constraint the fragmenter must ignore). Gate **67 -> 68**: `test/print_truth.cjs`, the first check here to look at print media -- page-count bands are **arithmetic**, derived from a height measured in the same run, and the last section's heading is read back out of the **PDF page tree** (flagship page **3 of 3**, consistency-models **7 of 7**). Three negative controls, two of which **abort** rather than fail an arm; the extractor's own control is executed against the committed before-PDFs (it reads them fine -- the red is the artifact's). **36 assertions RED pre-fix**, captured verbatim. VR **byte-identical, all 16** (`git diff` vs base empty; no `--update` ever run). `.gitattributes` gained `*.pdf binary`. **ROUND 2 (cold verify, w16-verifier):** the PRODUCT was confirmed exact and could not be broken; **2 BLOCKING defects were in the new CHECK** and are fixed. The PDF text extractor merged the inherited XObject map into every recursion, so `depth` counted a cross-product and the reader died past a cap of 4 -- and Chromium emits a variable number of Form XObjects for byte-identical input, so it was a coin flip (**5 forms healthy, 20 -> `[1657,0,0]` a FALSE FAILURE reported as an app defect, 24 -> `[0,0,0]`**); **5 of 8 standalone runs failed on a correct build**. And the page counter took an INTERMEDIATE `/Type /Pages` node while the tree walk took the most-kids node -- **the same wrong node**, so the arm that cross-checks them structurally could not catch it: print-qa is **11 pages, not 8**, the audit was right, and this report's "the difference is margins" was an explanation invented for my own bug (**retracted**). Also added: a coverage control for PARTIAL extractor death (gated on clipping, or it blames the reader for the app's truncation), a blank-page arm, and **ARM F** -- the native File->Print path with the sheet NEVER opened, the reason X1 was P1 and the one path nothing guarded. One correction back: the verifier's 4th mutant is a **no-op** (`.cs-sec:first-of-type` matches zero elements) and its apparent red was their own flake. **8/8 standalone PASS** post-fix (4 under load), all four mutants red for the stated reason, gate **68/68**, VR still 16/16 byte-identical. Verdict committed verbatim: `2026-07-30-w16-print-coldverify.md`.
- [2026-07-30 -- W17: the home's exits actually exit, and the keyboard overlay lets the keyboard in](2026-07-30-w17-exits.md) -- the cross-browser round's X3+X4 bundle, both engine-independent gate blind spots. **X3:** from `#home`, committing a Search result or clicking a Topic index card switched the registry and NAVIGATED NOWHERE -- `#home` and "Home" in the title while `TopicRegistry.current()` had moved, the hero still advertising the topic just left, zero console errors. The router's TOPICLESS guard is correct and untouched; the CALLER's navigate was missing on two of three pick surfaces. Fixed by giving the rule one home (`LastVisit.navigateAfterPick`) that all three now share, fired before `setTopic` so the switch costs no second history entry and still works when `setTopic` no-ops on `id === cur`. **X4:** `#keybody` was the one of four `.cram-body` dialog scroll regions without `tabindex="0" role="region"` -- 208px clipped, ONE tab stop across 12 real presses, `End` leaving scrollTop at 0, hiding the `Esc` and `?` rows from the keyboard user who went looking for them. The existing check passed because the hole was in its own denominator: an unfocusable body is not in the focusable set it compares against. Guarded now by a DOM-enumerated scroll-region **census** (with its own non-empty negative control) so the class cannot recur. 5+9 arms, watched red; 67/67 gate; VR 16/16 byte-identical, no baseline touched.
- [2026-07-31 hierarchy ledger](2026-07-31-hierarchy-ledger.md) — the Ledger Round's verdict: conductor + delegation pay; 0 bounces, 0 blocking; Sonnet implementers ACTIVATE next round
- [2026-08-01 -- W1 round 5: the closing round of the appeal home](2026-08-01-appeal-home-r5-addendum.md) -- five user-visible fixes, all of them one fact rendered twice. **The lesson is item 2:** the arm guarding the focus fix passed against BOTH mutants three times before it could fail -- a fresh-install page too short to strand focus (maxScroll 549 vs an 800px viewport), a FULLY-graded seed that is shorter still (668px -- a mature record collapses the home), a scroll silently undone by the app own restore, and an ordering (render-then-scroll) that no user produces and that clamps scroll to 0 by collapsing the document mid-rebuild. It now seeds a mid-campaign record, reports the scroll it actually achieved, and REFUSES to grade a run that could not stage the defect; negative control reads top -845, onScreen false. A post-layout reveal branch was measured, built, and then REMOVED -- it guarded only an ordering I had to write myself, and an unexercisable branch in an a11y path is a liability. Also: the tab pointer counted a display:none target as crossed, so it read "Library" from the top of the page down at every width where the bar is hidden. Gate 76/76; home pair rebaselined (33400/33460 px, one 624x169 box -- the off-ladder note leaving aria-hidden), the other 14 byte-identical to 1c533d7.
- [2026-08-02 journey enumeration](2026-08-02-journey-enumeration.md) -- stage A of the e2e journey-coverage audit: the product's 15 user journeys enumerated by black-box drive, blind to the test suite, with the 14 hand-measured defects that 13-of-14 landed in cells the coverage matrix marks UNWALKED/STATE-ONLY. Companion of record to 2026-08-02-e2e-journey-coverage.md (the matrix); committed late -- the synthesis landed first and this half was caught untracked by the W1.5 train's precondition check.
- [2026-08-02 -- W1.5 FREEZE: the home's refinements](2026-08-02-w15-freeze.md) -- four cycles, one closing pass (R7-R10) and two tail fixes from the marshal loop; **34 items, all closed**, gate **76 -> 77** and held. One new registered check (`home_fold`, 88 assertions over 22 record cells at two widths) plus arms on four existing ones (`overlay_deadzone` 36 -> 74, `focus_ring` 18 -> 24, `home_claims` 7 -> 13 planted mutants + 4 judges, `touch_floor` +2 sections). **Three instruments were caught lying and fixed rather than reported as coverage:** two checks cited for an ordinal swap that contain no ordering assertion at all; `home_fold` publishing a **57px scroll coin flip** whose numbers two parties had already quoted as facts; and a goal-sentence rule that was GREEN on the very defect it is now named for. Tail fixes: the home printed a **cram sheet for a topic the user never chose** (3 pages fresh / 6 pages after a visit -- now 2 pages of the record), and the goal bar **announced the sentence written directly underneath it** (CDP AX tree: `image` + `StaticText`, same words, adjacent -- now the fact appears once). Receipts: [gate](2026-08-02-w15-gate.txt), [VR pairs](w15-receipts/), full per-item measurements in `_WAVE_LEDGER.md` on `appeal/w15-refinements`. **DOUBLED COLD VERIFY** after the freeze: [fix verification](2026-08-02-w15-coldverify-fixes.md) -- 34/34 VERIFIED, 17 negative controls, 0 items disputing a fix (4 figures disputed, all of them the paperwork UNDERSHOOTING the work: the R1 compaction was 20.2px not 10px, a reversion reds 3 times not 1, the key reversion 26 not 22) -- and [hostile hunt](2026-08-02-w15-coldverify-hunt.md) -- 8 findings, whose through-line is that the arms were strong and the GAPS were verbs: **nothing in 77 checks pressed the goal stepper**, nothing enumerated what else lands on the home's paper, and the boot arm drove every key except the chord the same cycle fixed. Four fixed post-verify (a floating scroll-disc and an open Topic index both printing OVER the record; the stepper's aria-live destroyed on every press so the compensation for a removed accessible name could never fire, plus a silent clamp; the Ctrl+P chord failing OPEN in the boot window where the keymap fails CLOSED), three carried, six receipts corrected in place.
- [2026-08-02 -- W1.5 wave ledger (archived)](2026-08-02-w15-wave-ledger.md) -- the full four-cycle + closing-pass + tail-fix record of `appeal/w15-refinements`, moved here from `_WAVE_LEDGER.md` when W-ADDRESSES took that filename over. `_WAVE_LEDGER.md` at the repo root is always the CURRENT wave's ledger; closed waves live here beside their freeze.
- [2026-08-03 -- W-ADDRESSES: the home delight verdict, worked](../_WAVE_LEDGER.md) -- ten items from the home-delight check and the coverage audit, all ten closed. **The door now lights in the room you are returning to** (the home wore a stale `data-group`, so every `var(--acc)` consumer was lit in a boot constant while the Resume CTA alone knew the right room). **The gauge's severity ordering was INVERTED and one token swap did not fix it:** a keel is never drawn on the trough, so its loudness order flipped with the capsule fill it actually sits on -- the fill now stops 2px short and the mark paints into that gap, on a constant ground, which is what makes the ordering real (missed 4.64/8.21 vs shaky 3.67/3.62, both floors cleared, mutant-proven). Denominator 1.09 -> 4.10:1. Arrival reordered so the acts lead and the audit recedes (content order only, not one string moved). Depth 1.05 -> 1.33:1 with a **before/after** text-floor sweep proving no regression -- and correctly attributing three dark AA misses to master rather than to the wave. One new check, `craft_hygiene`, gate **77 -> 78**. Receipts + VR pairs in `w-addresses-home-before-after/`.
