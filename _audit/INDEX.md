# deepdive-rehearsal — audit index

- [2026-06-29 — Branch Merge-Triage](2026-06-29-branch-audit.md) — all 6 branches triaged in isolated worktrees + adversarial verify (13 agents). Verdicts: `enhance-web-components` **MERGE** (fixes latent unstyled-companion bug); `visual-enhancements` **SALVAGE** (visual track + the real guard tests master's RCA over-claims); the two giants v300/v308 = **one bloated lineage** (~88% generated JSON/PNG + offline-violating network code) → file-extract the ~1.2K-LOC SPA core only, never merge; `gh-pages` **REDEPLOY** (serving a stale unmerged commit); `master` keystone fix = `.gitattributes` EOL normalization. Recommended serial plan in the report (Step 0 foundation → enhance-web-components → visual-enhancements → SPA scope gate → SPA extract → gh-pages redeploy).
