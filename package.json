{
  "name": "deepdive-content-pipeline-rehearsal",
  "version": "1.0.0",
  "private": true,
  "description": "Dev/test tooling for the offline single-file interview trainer. The shipped deliverable is zero-dependency and self-contained; this package exists ONLY so THE GATE's browser checks (render / entity_leak / e2e_interactions) can run via Playwright locally and in CI.",
  "scripts": {
    "tokens": "style-dictionary build --config design-tokens/config.json",
    "build": "style-dictionary build --config design-tokens/config.json && vite build",
    "dev": "style-dictionary build --config design-tokens/config.json && vite",
    "preview": "vite preview",
    "gate": "python3 test/check_all.py",
    "browser-setup": "playwright install chromium",
    "e2e": "node test/render.cjs && node test/entity_leak.cjs && node test/e2e_interactions.cjs"
  },
  "devDependencies": {
    "@tailwindcss/vite": "^4.3.2",
    "gray-matter": "^4.0.3",
    "js-yaml": "^5.2.1",
    "markdown-it": "^14.3.0",
    "mermaid": "^11.16.0",
    "open-props": "^1.7.23",
    "playwright": "^1.40.0",
    "shiki": "^4.3.1",
    "style-dictionary": "^5.5.0",
    "vite": "^8.1.3",
    "vite-plugin-singlefile": "^2.3.3",
    "zod": "^4.4.3"
  },
  "dependencies": {
    "@material/material-color-utilities": "^0.4.0"
  }
}
