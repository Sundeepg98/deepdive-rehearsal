/* topics/content-pipeline/num.js -- FOUNDATION Phase-0 STUB. Unused at boot (the
   num pane stays plain and reads its own globals); the num agent seeds real data in Phase 1. */
var TOPIC_CP_NUM = { lead: '', tell: '', inputs: [], compute: function (vals, fmt) { return []; } };
