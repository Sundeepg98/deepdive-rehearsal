/* topics/content-pipeline/open.js -- FOUNDATION Phase-0 STUB. Unused at boot (the
   open pane stays plain and reads its own globals); the open agent seeds real data in Phase 1. */
var TOPIC_CP_OPEN = { cards: [] };
