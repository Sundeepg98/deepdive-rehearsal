/* topics/content-pipeline/rf.js -- FOUNDATION Phase-0 STUB. Unused at boot (the
   rf pane stays plain and reads its own globals); the rf agent seeds real data in Phase 1. */
var TOPIC_CP_RF = { lead: '', flags: [] };
