/* topics/content-pipeline/sys.js -- FOUNDATION Phase-0 STUB. Unused at boot (the
   sys pane stays plain and reads its own globals); the sys agent seeds real data in Phase 1. */
var TOPIC_CP_SYS = { intro: '', stages: [], pivots: [], heads: { whereHead: '', pivHead: '', pivSub: '' } };
