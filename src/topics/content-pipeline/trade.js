/* topics/content-pipeline/trade.js -- FOUNDATION Phase-0 STUB. Unused at boot (the
   trade pane stays plain and reads its own globals); the trade agent seeds real data in Phase 1. */
var TOPIC_CP_TRADE = { lead: '', decisions: [] };
