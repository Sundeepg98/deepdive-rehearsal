/* topics/content-pipeline/walk.js -- FOUNDATION Phase-0 STUB.
   Unused at boot: the walk pane stays plain and reads its own `steps` global. The
   walk agent replaces this with the real { steps, modelScript } arrays in Phase 1.
   This file is the manifest-tracked data home, so the agent never edits unit_tests.py. */
var TOPIC_CP_WALK = { steps: [], modelScript: [] };
