/* topics/content-pipeline/wb.js -- FOUNDATION Phase-0 STUB. Unused at boot (the
   wb pane stays plain and reads its own globals); the wb agent seeds real data in Phase 1. */
var TOPIC_CP_WB = { steps: [], diagram: '', foot: '', sub: '', okVerdict: '' };
