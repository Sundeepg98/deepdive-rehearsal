{
  "autoscaling::dup::rf.flags[4].fix + wb.steps[6].a": "Drain, then terminate. Stop sending it new work, wait for the deregistration to propagate, let it finish in-flight work,",
  "backpressure::dup::open.cards[0].items[1].a + trade.decisions[0].tell": "Shed to protect latency for interactive, loss-tolerant, retryable work; block (or spill to disk) to protect completeness",
  "circuit-breaker::dangling::open.cards[0].items[1].a": "Not recovering a single call, but containing the blast radius: paired with a fallback, one dependency's outage degrades",
  "consistency-models::when-conj::trade.decisions[4].opts[1].when": "when the home replicas are down, any available node accepts the write and hands it off later, so writes keep succeeding",
  "eav::when-conj::trade.decisions[5].opts[1].when": "When you want the database to type and index values natively -- numeric ranges, typed indexes -- at the cost of a wider, s",
  "eav::when-conj::trade.decisions[6].opts[0].when": "When a config change must be reviewed, or applied as a consistent set, so the device never runs a half-applied configura",
  "iac::when-conj::trade.decisions[3].opts[0].when": "When you want the config to stay close to declarative and legible -- the plan reflects the code, and reviewers read inten",
  "iac::when-conj::trade.decisions[3].opts[1].when": "When infrastructure genuinely needs real abstraction -- loops over complex data, typed constructs, unit tests, reuse of t",
  "kafka-internals::dup::open.cards[0].items[0].a + open.cards[0].items[1].a": "Kafka is a distributed append-only commit log: a topic is N retained, ordered partitions, producers append and consumers",
  "microfrontend::when-conj::trade.decisions[0].opts[1].when": "when release contention is measurably hurting you -- teams batching into a weekly train, blocked by each other's failures",
  "microfrontend::when-conj::trade.decisions[1].opts[0].when": "when deployment independence is what you're maximizing. Apps agree on a URL and a lifecycle and nothing else, so you can",
  "microfrontend::when-conj::trade.decisions[1].opts[1].when": "when apps share heavily (same framework, a big design system) and a platform team owns the build config. shared/exposes/",
  "microfrontend::when-conj::trade.decisions[2].opts[1].when": "when apps are genuinely isolated trees that never pass components, context, or hooks across the seam -- or temporarily, d",
  "microfrontend::when-conj::trade.decisions[6].opts[0].when": "when you genuinely cannot trust the code you're mounting (third-party or legacy apps). It is the only hard guarantee -- a",
  "multi-region::dup::rf.flags[5].fix + trade.decisions[4].tell": "Automate only when detection is confident (multiple failed checks, multiple vantage points, a real request path), failov",
  "notifications::dup::trade.decisions[5].opts[1].when + wb.steps[7].a": "In-app first; if unseen in a window, cascade to email; if opened in time, cancel the pending email. Reach the user once,",
  "replication::when-conj::trade.decisions[4].opts[1].when": "when you need to know whether two writes are truly concurrent or causally ordered, so you only resolve real conflicts --",
  "replication::when-conj::trade.decisions[4].opts[2].when": "when the data is naturally a merge -- a set, a counter, a cart, a collaborative document -- so concurrent updates converge",
  "rules-engine::dup::rf.flags[2].fix + wb.steps[4].a": "Two provably distinct approvers -- the second-approval step rejects both the requester and the first approver, not just t",
  "saga::dangling::open.cards[0].items[1].a": "It's ACD without isolation -- you trade atomicity for eventual consistency, so intermediate states are visible and compen"
}
