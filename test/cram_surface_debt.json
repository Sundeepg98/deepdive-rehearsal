{
  "api-design::dangling::wb.steps[0].a": "Because it gives a uniform, predictable surface: a consumer who knows one resource knows all of them, the verbs carry co",
  "autoscaling::dangling::wb.steps[1].a": "Because scale-up isn't instant (provisioning + cold start), the running fleet must absorb a spike during that lag. A tar",
  "autoscaling::dangling::wb.steps[5].a": "Because \"started\" isn't \"capacity.\" It has to boot, pull an image, warm its caches and connection pools, pass a readines",
  "autoscaling::dup::rf.flags[4].fix + wb.steps[6].a": "Drain, then terminate. Stop sending it new work, wait for the deregistration to propagate, let it finish in-flight work,",
  "backpressure::dangling::wb.steps[8].a": "It's only as strong as the weakest unbounded link -- if any hop has an unbounded queue, that hop silently absorbs the ove",
  "backpressure::dup::open.cards[0].items[1].a + trade.decisions[0].tell": "Shed to protect latency for interactive, loss-tolerant, retryable work; block (or spill to disk) to protect completeness",
  "caching::dangling::wb.steps[6].a": "It evicts. LRU is not scan-resistant -- one batch job evicts the hot set -- so LFU for skewed traffic. A rising eviction r",
  "cdc::dangling::wb.steps[1].a": "It changed which failure survives. A crash between publish and mark leaves the row unmarked, so it republishes -- a dupli",
  "circuit-breaker::dangling::open.cards[0].items[1].a": "Not recovering a single call, but containing the blast radius: paired with a fallback, one dependency's outage degrades",
  "circuit-breaker::dangling::wb.steps[0].a": "Because retrying a failing dependency amplifies the failure. Draw the loop before the machine: every waiting call holds",
  "circuit-breaker::dangling::wb.steps[5].a": "Because full traffic on a just-recovering dependency would re-overload it and cause a second outage -- the probe must be",
  "consistency-models::dangling::wb.steps[0].a": "Because replication is asynchronous: a write commits on the primary and propagates to the other replicas afterward, so f",
  "consistency-models::when-conj::trade.decisions[4].opts[1].when": "when the home replicas are down, any available node accepts the write and hands it off later, so writes keep succeeding",
  "consistent-hashing::dangling::wb.steps[2].a": "Because ownership is \"first node clockwise,\" so a joining node claims only the arc between it and its predecessor, and a",
  "consistent-hashing::dangling::wb.steps[5].a": "No. A decommission streams its ranges to the successors before it leaves. A failure hands ownership instantly to success",
  "debugging::dangling::wb.steps[0].a": "Because dashboards are built on the success table, and a pipeline that stores only successfully-processed records report",
  "developer-platform::dangling::wb.steps[4].a": "Because a golden path you can't leave pushes the first non-fitting case off the platform and out of the guardrails. So:",
  "eav::when-conj::trade.decisions[5].opts[1].when": "When you want the database to type and index values natively -- numeric ranges, typed indexes -- at the cost of a wider, s",
  "eav::when-conj::trade.decisions[6].opts[0].when": "When a config change must be reviewed, or applied as a consistent set, so the device never runs a half-applied configura",
  "error-propagation::dangling::wb.steps[6].a": "You cannot tell from the code alone -- the code says what, a correlation id propagated across every hop says where. Mint",
  "error-propagation::dangling::wb.steps[8].a": "Because the default behavior at every boundary is catch-log-rethrow-generic -- each service wraps whatever it caught in i",
  "event-driven::dangling::wb.steps[7].a": "Not global order -- you don't need it and it doesn't scale. Partition by the entity key for per-entity order, and make th",
  "iac::when-conj::trade.decisions[3].opts[0].when": "When you want the config to stay close to declarative and legible -- the plan reflects the code, and reviewers read inten",
  "iac::when-conj::trade.decisions[3].opts[1].when": "When infrastructure genuinely needs real abstraction -- loops over complex data, typed constructs, unit tests, reuse of t",
  "idempotency::dangling::wb.steps[0].a": "Because failure is ambiguous: a timeout doesn't say whether the request or the response was lost. And the check races to",
  "idempotency::dangling::wb.steps[4].a": "It races. Two concurrent duplicates both find no key, both proceed, both charge. Only an atomic claim closes it. And a d",
  "idempotency::dangling::wb.steps[5].a": "Because a crash between them breaks the guarantee in one of two directions: effect-then-key double-charges on the retry;",
  "kafka-internals::dangling::wb.steps[1].a": "Both ordering and load, in one choice. hash(key) % N picks the partition, so same key -> same partition -> ordered. Skewed",
  "kafka-internals::dangling::wb.steps[3].a": "Because a partition is assigned to exactly one consumer in the group. A 13th consumer on 12 partitions gets zero partiti",
  "kafka-internals::dangling::wb.steps[7].a": "Because the ISR shrinks. If it collapses to just the leader, \"all in-sync replicas\" means one. Durability is a chain of",
  "kafka-internals::dup::open.cards[0].items[0].a + open.cards[0].items[1].a": "Kafka is a distributed append-only commit log: a topic is N retained, ordered partitions, producers append and consumers",
  "leader-election::dangling::wb.steps[0].a": "Because the work doesn't commute: ordering writes, owning a resource, running a job once. If the work did commute -- appe",
  "leader-election::dangling::wb.steps[1].a": "You cannot distinguish a dead node from a slow one. No timeout is provably long enough, so \"the leader is dead\" is perma",
  "load-balancing::dangling::wb.steps[1].a": "Because all traffic flows through it, so a single LB is a single point of failure -- it needs an active-passive pair, DNS",
  "load-balancing::dangling::wb.steps[6].a": "Because it is the slowest instance in the pool -- empty caches, cold connection pools, no JIT -- while least-connections s",
  "load-balancing::dangling::wb.steps[8].a": "Because a shared dependency failing makes every instance's deep check fail at once, so the LB evicts the entire pool and",
  "microfrontend::when-conj::trade.decisions[0].opts[1].when": "when release contention is measurably hurting you -- teams batching into a weekly train, blocked by each other's failures",
  "microfrontend::when-conj::trade.decisions[1].opts[0].when": "when deployment independence is what you're maximizing. Apps agree on a URL and a lifecycle and nothing else, so you can",
  "microfrontend::when-conj::trade.decisions[1].opts[1].when": "when apps share heavily (same framework, a big design system) and a platform team owns the build config. shared/exposes/",
  "microfrontend::when-conj::trade.decisions[2].opts[1].when": "when apps are genuinely isolated trees that never pass components, context, or hooks across the seam -- or temporarily, d",
  "microfrontend::when-conj::trade.decisions[6].opts[0].when": "when you genuinely cannot trust the code you're mounting (third-party or legacy apps). It is the only hard guarantee -- a",
  "multi-region::dangling::wb.steps[8].a": "You do not, until you have failed over under a game day and hit your RTO/RPO. An untested plan is an assumption: replica",
  "multi-region::dup::rf.flags[5].fix + trade.decisions[4].tell": "Automate only when detection is confident (multiple failed checks, multiple vantage points, a real request path), failov",
  "multi-tenant::dangling::wb.steps[2].a": "It returns more rows -- no error, no crash. It fails open and it fails silently, and more rows looks exactly like success",
  "notifications::dup::trade.decisions[5].opts[1].when + wb.steps[7].a": "In-app first; if unseen in a window, cascade to email; if opened in time, cancel the pending email. Reach the user once,",
  "probabilistic-structures::dangling::wb.steps[0].a": "Because a Bloom filter only ever sets bits to 1, never clears them. Adding an element sets its k hash-positions to 1, an",
  "probabilistic-structures::dangling::wb.steps[4].a": "Because collisions only add to counters, never remove. Each of the d rows holds one counter for the element (at a hash-c",
  "rate-limiting::dangling::wb.steps[7].a": "Its counter is a hot key on one shard -- a throughput bottleneck. Shard into sub-keys, or a local bucket with periodic sy",
  "real-time-delivery::dangling::wb.steps[7].a": "It grows without bound -- messages queue in that connection's send buffer, and a client can make you allocate memory inde",
  "replication::dangling::wb.steps[5].a": "Because a write set of W nodes and a read set of R nodes, together exceeding N, must overlap in at least one node -- so t",
  "replication::when-conj::trade.decisions[4].opts[1].when": "when you need to know whether two writes are truly concurrent or causally ordered, so you only resolve real conflicts --",
  "replication::when-conj::trade.decisions[4].opts[2].when": "when the data is naturally a merge -- a set, a counter, a cart, a collaborative document -- so concurrent updates converge",
  "retries-timeouts::dangling::wb.steps[8].a": "Not waiting, and not adding capacity -- the system is metastable, saturated by retry traffic it now generates itself, so",
  "rules-engine::dangling::wb.steps[5].a": "It expires on a time-box, and its status is derived from the record, so nothing lingers partially armed or ambiguous.",
  "rules-engine::dup::rf.flags[2].fix + wb.steps[4].a": "Two provably distinct approvers -- the second-approval step rejects both the requester and the first approver, not just t",
  "saga::dangling::open.cards[0].items[1].a": "It's ACD without isolation -- you trade atomicity for eventual consistency, so intermediate states are visible and compen",
  "saga::dangling::wb.steps[2].a": "Because each local transaction already committed and its effect is visible -- you can't un-commit it, so you counteract i",
  "saga::dangling::wb.steps[3].a": "Only the ones before the pivot. You order the saga so the cheap, reversible steps come first (the compensatable prefix --",
  "saga::dangling::wb.steps[6].a": "Because they are two writes, and a crash between them either stalls the saga (committed, no event, nobody advancing it)",
  "sharding-strategies::dangling::wb.steps[1].a": "Because it controls the two things that make or break a sharded system: distribution (does load and data spread evenly,",
  "signing::dangling::wb.steps[5].a": "It hashes the payload, then verifies the header signature against its burned-in public key. Match -> trusted; mismatch or",
  "slos::dangling::wb.steps[5].a": "It measures how fast you're spending the budget versus the sustainable rate, so you can alert on multiple windows -- a fa",
  "soft-delete::dangling::wb.steps[4].a": "Only that the parent row exists -- not that it's live. Draw a new child arrow landing on a greyed-out (deleted) parent an",
  "soft-delete::dangling::wb.steps[5].a": "No. Cascading is a 50,000-row write inside a user-facing transaction, and 50,000 more to undo on restore. Mark the paren",
  "state-machine::dangling::wb.steps[4].a": "Not fired inline. A transition-outbox row is written in the same transaction as the state change, and a relay sends it a",
  "storage-engines::dangling::wb.steps[1].a": "It merges SSTables into fewer, larger sorted files, keeping the newest value per key and dropping obsolete/deleted entri"
}
